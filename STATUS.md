# Status do Projeto: ThunderaFit (SaaS Fitness)

> Log macro por fase — 1-3 linhas cada, sem evidência bruta (a evidência real fica
> registrada na conversa de execução de cada fase). Ver `MASTER_SPEC.md` para o
> estado técnico vigente, decisões de arquitetura e roadmap priorizado.

## Resumo por fase

**Fase 1 — Fundação Core, Auth e Estrutura Modular.** Backend Fastify+Prisma+Postgres, JWT access+refresh, estrutura modular domain-first.

**Fase 2 — Vínculo Personal↔Aluno e Limite Freemium.** `ClientRelation`, limite de 3 alunos grátis por Personal. *Modelo: gpt-oss:20b (Ollama Cloud).*

**Fase 3 — Catálogo de Exercícios e Prescrição de Treinos.** Seed de 29 exercícios; `Exercise`/`Workout`/`WorkoutExercise`.

**Fase 4 — Execução do Treino e Registro de Carga.** `SetLog`; `GET /api/workouts`.

**Fase 5 — Frontend Web Mobile-First.** Next.js App Router; design system "Voltagem"; telas core; E2E inicial (Puppeteer).

**Fase 5.5 — Hardening Pré-Produção.** Tokens em cookies httpOnly; CORS; `dev.sh`/`dev.ps1`.

**Fase 6 — Painel do Personal Trainer (UI).** Dashboard, vínculo de aluno por e-mail, criação de treino pela UI.

**Fase 7 — Suíte de Testes Automatizados de Frontend.** Jest+RTL e Playwright substituindo scripts ad-hoc.

**Fase 8 — Evolução (Histórico de Carga e Frequência).** Domínio `/progress`; gráficos Recharts.

**Fase 9 — Consolidação.** Mobile-first confirmado; stub de billing; fix de telas travadas em "Carregando".

**Fase 10 — Anamnese + Dúvidas + Notificações.** 3 domínios novos; bug de `Content-Type` em request sem corpo corrigido (afetava desde a Fase 5.5).

**Fase 11 — Módulo de Nutrição & Multi-Profissional.** Role `NUTRICIONISTA`; `DietPlan`/`DietMeal`/`DietFood`; dashboard do aluno unificado.

**Fase 12 — Polish Visual & Usabilidade.** Seleção de perfil na home; acento de cor por papel; convite copiável ao vincular aluno inexistente.

**Fase 13 — Deploy em Produção (GCP + Terraform + Neon).** Containers + proxy autenticado; infra via Terraform. Bug crítico corrigido: prioridade do cookie de sessão sobre o header `Authorization` do proxy.

**Fase 14 — Painel Administrativo.** Role `ADMIN`; rate limit de login em memória (IP+e-mail); domínio `/admin` (rota oculta `/nimbus`); `AdminAccessLog` para leituras de anamnese.

**Fase 15 — Catálogo de Exercícios Expandido.** 29→149 exercícios; `difficultyLevel`; filtro por grupo muscular.

**Fase 16 — Programas de Treino (Templates + Sessões + Progresso).** `WorkoutProgram` com sessões A-E; aplicar = cópia (nunca referência); `suggestedNext`.

**Fase 17 — Consolidação de Pendências.** 6 frentes: dashboard alinhado ao `suggestedNext`, mídia na prescrição, player responsivo, auditoria de segurança (2 gaps de role corrigidos), `DietPlan.isActive`, anamnese/dúvidas para o Nutricionista.

**Fase 18 — UX de Autenticação + Remoção do Nutricionista da UI.** Diferenciação visual login/cadastro; bug real de sessão corrigido (`sessionStorage`→`localStorage`); Nutricionista removido da seleção pública (backend intacto).

**Fase 19 — Spike Capacitor + Auth por Cookie.** Viabilidade do app Android confirmada com ajuste (`CookieManager.flush()` no lifecycle).

**Fase 20 — Billing Real (Stripe Checkout).** Integração completa + webhook assinado + hardening adversarial; deployado porém inerte (sem chaves `STRIPE_*`).

**Fase 21 — Descoberta de Profissionais + Reavaliação de UI.** Domínio `/connections`; busca por localização; aprovação manual do vínculo.

**Fase 22 — Reconciliação de Branches.** Fases 19/20/21 integradas em `dev`+`main`; deploy em produção.

**Fase 24 — Fluxo de Auth Unificado.** `/login` vira fluxo de 4 etapas via `check-email`; `/register` removido; rate limiter corrigido (chave IP+e-mail, não IP puro).

**Fase 25 — Correção do Fluxo Programa→Sessões.** Tela obsoleta `/personal/treinos/novo` removida; dashboard aponta pro fluxo de Programas (já correto desde a Fase 16); mitigação leve no player de vídeo.

**Fase 26 — Esquema de Sessões (Letras/Dias) + Tela Dedicada por Sessão.** `sessionScheme` (LETTER/WEEKDAY); bug de ordenação alfabética corrigido (`suggestedNext` dependia de `localeCompare`); tela própria por sessão substitui o acordeão inline.

**Fase 27 — Feedback de Exercício Adicionado + Observações por Prescrição.** Toast de confirmação; campo `notes` (500 caracteres) por exercício prescrito. *Sem suíte de testes rodada nesta fase (pendência aberta).*

**Fase 28 — Polish do Formulário de Exercício.** Bug real corrigido: o botão "posição N" ficava travado em 1 na tela de sessão (invalidava a query errada, Fase 26); popup de confirmação maior e centralizado; reordenar exercícios prescritos com setas ↑/↓ (`POST /api/workouts/:id/exercises/:exerciseId/move`, sem migration). *Modelo: Sonnet 5. Backend verde (5 testes novos; único módulo com falha é `src/admin`, timeout de ambiente pré-existente e não relacionado), 22 Jest/RTL, 21/21 Playwright (novo `exercicio-polish-flow.spec.ts`).*

**Fase 28.1 — Hotfix pós-relato.** Investigação do "trava ao mover a mesma seta" não reproduziu corrupção de dados nem freeze (testado via API concorrente e E2E com cliques rápidos repetidos) — mesmo assim, endurecido: `moveExercise` agora lê+troca dentro de uma única transação (fechava uma janela de corrida teórica) e erros deixaram de ser silenciosos no botão. Investigação do "programa novo não aparece" confirmou que os dados estão corretos em toda API testada — o gap real era `/personal/programas` não mostrar qual aluno cada instância aplicada pertence; corrigido (mostra o e-mail do aluno em cada card). *Modelo: Sonnet 5.*

**Fase 29 — Hub de Administração do Aluno.** Nova tela `/personal/alunos/[alunoId]` reunindo programas aplicados, evolução (carga/frequência) e link pra anamnese — antes o Personal criava um programa e não tinha pra onde voltar. Pré-requisito corrigido: `/api/progress/*` rejeitava PERSONAL com 403; ganhou um ramo com checagem de `ClientRelation` (mesmo padrão do domínio anamnesis), fechando o IDOR que existiria se fosse liberado sem checar vínculo. `GET /api/workout-programs` ganhou filtro opcional `?alunoId=`, sempre ANDado com o `personalId` do JWT. Dashboard do Personal: link "Anamnese" virou "Gerenciar →" pro hub. *Modelo: Sonnet 5. 184/184 backend (7 testes novos), 22 Jest/RTL, 23/23 Playwright (novo `hub-aluno-flow.spec.ts`, incluindo o bloqueio a aluno não vinculado).*

**Fase 30 — Foto de Perfil (aluno e Personal).** Avatar circular pequeno no `AppHeader`, com upload em `/perfil` (novo, aluno) e `/personal/perfil`. Decisão de armazenamento: **banco, não bucket** — o redimensionamento/crop quadrado acontece inteiramente no cliente (canvas, 256px, WebP/JPEG ~0.82 qualidade) antes do upload, resultando em poucos KB; guardado como data URI num `WorkoutExercise`-like `User.avatarUrl String?` (migration aditiva). Backend valida de novo (formato + cap de 140KB), nunca confia só no cliente. Novo endpoint `PUT /api/auth/me/avatar` (qualquer role autenticada; `null` remove). Componentes `AvatarUpload`/`UserAvatar` desacoplados de qualquer domínio (Seção 0: reusabilidade). *Modelo: Sonnet 5. 190/190 backend (6 testes novos), 22 Jest/RTL, 25/25 Playwright (novo `avatar-perfil-flow.spec.ts`, upload real via `setInputFiles`, persistência após reload, remover).*

**Fase 31 — Consolidação: Dashboard Agrupado + Exclusão de Programas/Templates + Correção do Avatar.** Três bugs reais relatados via screenshots do celular. Dashboard do Personal: "Treinos prescritos" deixou de ser uma lista plana de sessões soltas e passou a agrupar por `WorkoutProgram` (nome do programa como cabeçalho, sessões aninhadas dentro). Novo `DELETE /api/workout-programs/:id` (template ou instância aplicada; 404/403 iguais a `apply`; cascata manual em transação, sem `onDelete: Cascade` no schema); componente `DeleteProgramButton` com confirmação inline, usado em `/personal/programas`, no hub do aluno e no dashboard. Avatar: causa raiz era o link "Perfil" do `AppHeader` ficar invisível abaixo do breakpoint `sm` (sem caminho nenhum até `/perfil` no celular); o próprio ícone circular virou botão com popover de upload, em qualquer largura de tela. *Modelo: Sonnet 5. 195/195 backend (5 testes novos de delete), 22 Jest/RTL, 29/29 Playwright (2 specs novos: `dashboard-consolidado-delete-flow.spec.ts` + teste extra em `avatar-perfil-flow.spec.ts` via popover do header).*

**Fase 32 — Infraestrutura de Mídia de Exercícios (bucket GCS + player enquadrado).** `Exercise.mediaUrl` antes só resolvia link do YouTube. Novo campo `Exercise.mediaType` (`YOUTUBE | VIDEO | GIF`, default `YOUTUBE` — compatibilidade retroativa com o catálogo existente). Diferente do avatar (Fase 30, banco OK pra blob pequeno por-usuário), mídia de exercício é catálogo fixo servido repetidamente a todos os usuários — vai pro **bucket GCS** (`infra/storage.tf`, objeto público read-only, só o service account do backend escreve), não pro Postgres. Backend: `PUT /api/admin/exercises/:id/media` (`assertAdmin`, `bodyLimit` de 8MB só nesta rota), valida existência → formato (regex por tipo) → tamanho → decodifica base64 → sobe pro bucket → persiste a URL; nunca confia no `mediaType` que o cliente declara sem checar o formato de verdade do arquivo. Frontend: `ExerciseExecutionCard` ganha branches por `mediaType` — VIDEO renderiza `<video autoplay loop muted playsinline>` (replica UX de GIF sem o custo de arquivo, decisão registrada no MASTER_SPEC: GIF de verdade infla um clipe H.264 de ~900KB pra 5-12MB), GIF renderiza `<img>` direto; YOUTUBE mantém o fluxo de embed já existente. *Modelo: Sonnet 5. 204/204 backend (9 testes novos de mídia), 25/25 Jest/RTL frontend (3 testes novos de mediaType). Infra: `infra/storage.tf` aplicado via Terraform, API `storage.googleapis.com` habilitada, `GCS_BUCKET_NAME` injetado no Cloud Run do backend.*

**Fase 32.1 — Hotfix: E-mail Visível no Cadastro + Mostrar Senha no Login.** Bug real reportado durante o teste manual da Fase 32: a etapa "Vamos criar sua conta" (`/login`, quando o e-mail digitado ainda não tem conta) não mostrava o e-mail digitado — diferente das outras etapas do mesmo fluxo, que já mostravam. Corrigido. Aproveitado pra adicionar um botão "Mostrar/Ocultar" nos campos de senha (login e cadastro), pra conferir o que foi digitado quando o login falha por senha errada, sem depender do gerenciador de senha do navegador. *Modelo: Sonnet 5. 25/25 Jest/RTL (sem teste novo — mudança de UI simples).*

**Fase 33 — Admin: CRUD do Catálogo de Exercícios (tela dedicada) + Edição de Role.** `/src/admin` era só leitura; ganhou camada de escrita completa em `/api/admin/exercises` (create/update/delete, rota separada da pública `GET /api/exercises` que continua só-leitura) e `PUT /api/admin/users/:id/role`. Nova tela dedicada `/nimbus/exercicios` (form inline de criar/editar, exclusão com confirmação — mesmo padrão do `DeleteProgramButton` da Fase 31). Guardrails: categoria (`muscleGroup`) vira dropdown das já existentes no banco, criar categoria nova é uma ação separada e explícita; nome **parecido** (variação de acento/espaço/caixa, ou erro de digitação de até 2 caracteres via distância de Levenshtein) pede confirmação explícita antes de salvar (nome idêntico já é barrado pelo `@unique`); exclusão bloqueada (409) se o exercício está em uso em alguma prescrição (`WorkoutExercise`), evitando corromper histórico de séries de alunos — mesmo raciocínio de cascata manual da Fase 31. Edição de role em `/nimbus/usuarios`: bloqueia auto-edição e remoção do último ADMIN do sistema, audita em nova tabela `AdminAuditLog` (separada de `AdminAccessLog`, que é especificamente sobre acesso a anamnese) — exibida junto em `/nimbus/logs-acesso`. *Modelo: Sonnet 5. 222/222 backend (18 testes novos: 12 de CRUD de exercício, 6 de role/auditoria), 25/25 Jest/RTL, 29/29 Playwright (`admin-flow.spec.ts` estendido com CRUD de exercício + edição de role + checagem de auditoria).*

**Fase 33.2 — Hotfix: Menu Mobile + Avatar/E-mail no Hub do Aluno.** Três bugs reais reportados via celular. (a) Todos os links de navegação do `AppHeader` (Programas, Evolução, Anamnese, etc.) eram só `sm:inline` — no celular não existia NENHUM jeito de navegar entre seções além do botão voltar do navegador; corrigido com um menu hambúrguer (`sm:hidden`) alimentado pela mesma lista de links usada na barra desktop (`NAV_LINKS_BY_ROLE`), sem duplicar JSX por papel. (b) No hub do aluno (`/personal/alunos/[alunoId]`), o e-mail do aluno estourava a largura da tela no celular (`text-2xl` sem quebra, e-mail não tem espaços pra quebrar linha naturalmente) — corrigido com `break-all` + tamanho responsivo. (c) Adicionado o avatar do aluno (já existente desde a Fase 30) no cabeçalho do hub, antes só visível pro próprio usuário — `GET /api/relations` passou a incluir `avatarUrl` na resposta. *Modelo: Sonnet 5. 222/222 backend, 25/25 Jest/RTL, 30/30 Playwright (novo `mobile-nav-flow.spec.ts`, viewport de celular real).*

**Fase 33.3 — Curadoria do Catálogo de Exercícios (destaque por grupo + novas categorias).** O catálogo de ~150 exercícios (Fase 3) tinha um problema real de usabilidade: grupos com várias variações do mesmo movimento (10 "Supino", 7 "Agachamento") apareciam em ordem alfabética sem nenhum destaque pro exercício mais relevante. Pesquisa de popularidade real de academia (StrengthLog e outras fontes) mapeou os ~5 exercícios mais feitos de cada um dos 7 grupos musculares, marcados com nova flag `Exercise.isFeatured` (editável em `/nimbus/exercicios`, Fase 33) — aparecem primeiro dentro do grupo (`orderBy: [{isFeatured:"desc"},{name:"asc"}]` centralizado no repository público) e ganham destaque visual (borda esquerda + selo ☆ em `accent-secondary`, cor diferente do `accent` já usado pro estado "selecionado") na tela do Personal que escolhe o exercício. Duas categorias novas: Alongamento e Cardio (10 exercícios cada, do zero) + 2 extras em Abdômen (Dead Bug, Prancha com Rotação) — catálogo cresce de 149 para 171. Recategorizado "Levantamento Terra Romeno" de Costas para Pernas (inconsistente com "Levantamento Terra Sumô", já em Pernas). **Achado de segurança de dados durante o planejamento**: `prisma/seed.ts` fazia `upsert` com `update` em TODOS os campos toda vez que rodava — qualquer edição manual via `/nimbus/exercicios` ou mídia customizada (Fase 32) seria revertida silenciosamente no próximo `npm run db:seed`; corrigido para create-only (só cria o que não existe, nunca sobrescreve). *Modelo: Sonnet 5. 222/222 backend, 25/25 Jest/RTL, 30/30 Playwright.*

**Fase 33.4 — Redesign do Dashboard do Aluno (hero contextual + voltagem semanal + métricas rápidas).** Três problemas de design no dashboard: (a) hero repetia "Sessão {letra} — {nome}" sem contexto real pro aluno decidir se começa agora — agora mostra nome do programa, rótulo da sessão (`labelFor`, reaproveitado do esquema Letras/Dias), contagem de exercícios e tempo estimado (heurística client-side: `sets × (40s + restSeconds)`, sem migration); (b) nada comunicava "sequência de dias" — nova **barra de voltagem semanal** (`WeeklyVoltageBar`), extensão do `VoltageBar` já existente: 7 blocos, dourado nos dias com série registrada, contorno tracejado no dia atual; (c) `EvolucaoTeaser` (card grande, quase sempre vazio) removido, substituído por 2 métricas rápidas (`WeeklyStats`): volume da semana em kg (ciano — "dado positivo") e sequência de dias (dourado — mesmo tom do CTA "Começar treino"). Rodapé de atalhos (Evolução/Anamnese/Dúvidas) virou 3 ícones de peso igual em violeta (`--role-nutricionista`, reaproveitado só pelo tom). Novo endpoint `GET /api/progress/weekly-summary` (mesmo domínio/padrão de `getFrequency`/`getLoadHistory`) — janela de 90 dias pra sequência real (não capada em 7), volume só dos últimos 7. *Modelo: Sonnet 5. 225/225 backend (3 testes novos), 25/25 Jest/RTL, 30/30 Playwright.*

**Fase 35 — Card de Resumo Pós-Treino (motivação in-app + exportável pro Instagram Stories).** Uma peça, dois usos: ao concluir uma sessão (antes só trocava o rótulo do botão em cima da mesma tela), um modal mostra o card de resumo — e o MESMO componente é exportado como imagem 9:16 via captura de DOM (`html-to-image`, não geração server-side, que exigiria um segundo template). Sem migration: não existe entidade de "sessão" no banco (`Workout.lastCompletedAt` é um único timestamp sobrescrito a cada conclusão), então o resumo é derivado por heurística de janela de tempo — séries "desta sessão" são as logadas depois da conclusão anterior (teto de segurança de 6h). Hero: volume total (ciano, mesma semântica de "dado positivo" da Fase 33.4) + comparação com a conclusão anterior do MESMO Workout (ou framing de "primeira vez" sem sessão/volume anterior). Selos de PR (dourado, até 2 + overflow "+N recordes"): maior peso já levantado pro exercício, comparado contra todo o histórico do aluno (cross-programa) — exercício sem nenhum histórico anterior não conta como PR (evita spam em exercício novo do programa). `POST /api/workouts/:id/complete` (não criou endpoint novo) agora retorna `{ workout, summary }`; nova marca vetorial `BoltMark` substitui o emoji ⚡ no rodapé do card (emoji rasteriza inconsistente na captura). Sem personalização por nome (não existe campo de nome no cadastro hoje — fica pra um plano futuro de cadastro mínimo de primeiro acesso). *Modelo: Sonnet 5. 227/227 backend (10 testes novos de `workout-summary.service` + 2 de integração), 36/36 Jest/RTL (11 testes novos), 31/31 Playwright (novo `post-workout-summary-flow.spec.ts`).*

**Fase 36 — PR em Tempo Real + Comparação de Volume (fundação de dado).** Duas peças de lógica pura, sem tela nova (a UI já existente da Fase 35 só foi ajustada pra não quebrar com a mudança de contrato). (a) **PR por série**: `POST .../logs` passa a retornar `isPersonalRecord`/`previousBest` — comparado ANTES de gravar a nova série, contra o maior peso já registrado pelo aluno pro exercício (reps não entram na conta; primeira vez no exercício nunca é PR, sem baseline). Coexiste com a lista de PRs por sessão que já existia (dois momentos de UX diferentes, não duplicação). (b) **Comparação de volume**: já existia igual ao pedido (mesmo `Workout`, nunca sessões diferentes) — só renomeei os campos do `summary` pra `hasHistory`/`previousVolumeKg`/`volumeChangePercent`, no lugar do antigo `comparison: {type,...}`. Novo: `streakDays` no resumo (reaproveita a mesma lógica de sequência do dashboard, `progress.service.ts::getWeeklySummary`) e `durationMinutes` aproximado (última série − primeira série da sessão, sem migration — subestima o aquecimento antes da 1ª série; duração exata exigiria um campo novo tipo `Workout.currentSessionStartedAt`, não aplicado). Decisão registrada com o fundador: não criar `GET /api/workouts/:id/summary` separado como cogitado inicialmente — enriquecer o `summary` já embutido no `POST /complete` evita duas fontes de verdade calculando a mesma coisa. *Modelo: Sonnet 5. 250/250 backend (14 testes novos: 12 em `workout-summary.test.ts` + 3 de PR em `setlogs.test.ts`), 36/36 Jest/RTL, Playwright sem regressão.*

**Fase 37 — Compartilhamento Nativo no Instagram + Redesign do Card Pós-Treino.** (a) Card: hero deixa de ser volume total e vira **contagem de séries**; abaixo, grid de 3 métricas secundárias menores — Duração, "Peso levantado Hoje" (era o volume) e Dias seguidos. (b) Compartilhamento: botão "Compartilhar" **unificado** (não um 3º botão "Compartilhar no Instagram" separado) — decide sozinho o mecanismo por baixo dos panos: dentro do Capacitor (`Capacitor.isNativePlatform()`) grava o PNG num arquivo temporário via `@capacitor/filesystem` (`Directory.Cache`) e abre o share sheet nativo via `@capacitor/share` (o Instagram aparece como opção se instalado, sem OAuth/API key); fora do Capacitor, mantém a Web Share API de antes. Decisão registrada com o fundador: usar o share sheet genérico do `@capacitor/share` em vez do deep link `instagram-stories://` — o deep link exigiria escrever um plugin nativo do zero (Kotlin + FileProvider manual no Android, Swift + UIPasteboard no iOS) e não há projeto iOS no repo ainda; o share sheet genérico é first-party, funciona hoje, e o "Instagram não instalado" já é tratado de graça (some da lista, sem erro). Achado de sorte: `AndroidManifest.xml` já tinha um `FileProvider` configurado com o authority certo e cobrindo o diretório de cache (resquício do scaffold original do `cap add android`) — zero config nativa manual, só `npx cap sync android` pra registrar os plugins novos. *Modelo: Sonnet 5. 42/42 Jest/RTL (6 testes novos: share nativo mockado + fallback de erro), Playwright sem regressão. Teste real do fluxo nativo (Instagram recebendo a imagem de verdade) requer dispositivo/emulador Android — não coberto por automação.*

**Fase 38 — Campo de Reps Limitado a 2 Dígitos + Avanço Automático de Foco.** Ajuste pequeno no formulário de registro de série (`ExerciseExecutionCard`): o campo de reps trunca o valor em 2 dígitos (ninguém faz mais de 99 repetições numa série) e, ao completar o 2º dígito, move o foco automaticamente pro campo de carga seguinte — sem precisar de Tab manual. *Modelo: Sonnet 5. 42/42 Jest/RTL (1 teste novo), Playwright sem regressão.*

**Fase 39 — Grupos A e B do backlog pós-lançamento (sugestão de treino, dashboard, card pós-treino, cadastro mínimo de nome).** Levantamento crítico de 7 pedidos do fundador cruzado com o roadmap pendente do `MASTER_SPEC.md`, agrupado em 5 grupos (A-E) pra não fazer tudo de uma vez — decisão registrada de por qual começar antes de qualquer código. **Grupo A:** (1) bug real corrigido em `computeSuggestedSessionId` — esquema WEEKDAY usava a MESMA lógica de round-robin do LETTER, ignorando o dia da semana atual; agora a sugestão pra WEEKDAY é SEMPRE a sessão do dia de hoje (determinístico, sem depender de histórico de conclusão; sem sessão cadastrada pro dia, não sugere nada). LETTER continua com o round-robin de antes. (2) dashboard do aluno troca "Volume da semana" (kg) por "Séries na semana" (`setsThisWeek`, novo campo em `getWeeklySummary`) — leitura mais direta de esforço semana a semana que kg (que varia muito com o exercício/fase do mesociclo). **Grupo B:** (3) card pós-treino ganha cabeçalho personalizado ("{Nome} mandou bem no treino! 💪" no lugar de "Treino A") — dependia de um dado que não existia (nome no cadastro), então **puxamos pra frente** um cadastro mínimo (`User.name String?`, nullable/sem backfill, capturado como campo obrigatório no formulário de "Criar conta" mas opcional na API pra não quebrar os 17 arquivos de teste que usam `/register` só como fixture); contas antigas caem no mesmo fallback de prefixo de e-mail já usado na saudação do dashboard (`firstNameOrEmailPrefix`, novo helper reaproveitado nos dois lugares). (4) duração do card deixa de ser aproximada (backend, primeira↔última série) e vira um **cronômetro real client-side** — timestamp de abertura da tela de execução até o clique em "Concluir", formatado H:MM:SS, sem precisar de campo novo no banco nem endpoint. Sequência ("dias seguidos") já existia (Fase 36) e já lidava certo com múltiplas conclusões no mesmo dia — só confirmado, não mudou. *Modelo: Sonnet 5. 253/253 backend (testes de WEEKDAY reescritos pra validar dinamicamente contra o dia real, 2 novos de cadastro com/sem nome), 43/43 Jest/RTL, 31/31 Playwright.*

**Fase 40 — Grupo C: Bug Real Corrigido no Registro de Série (corte de sessão) + Referência da Última Vez.** Achado mais sério que o esperado ao investigar o item "reps/carga devem zerar numa sessão nova": `WorkoutExercise.setLogs` sempre retornou o histórico INTEIRO (todas as semanas), não só a sessão atual — porque o mesmo `Workout`/`WorkoutExercise` é reaberto toda semana, nunca recriado. Sem corte por data, depois da 1ª semana completa o formulário de registrar série sumia PRA SEMPRE (`setLogs.length` já batia o `sets` prescrito com séries de semanas passadas — `isComplete` ficava `true` permanentemente). Corrigido 100% no frontend, sem migration: novo helper `splitSetLogsBySessionBoundary` usa `Workout.lastCompletedAt` de ANTES da sessão atual como corte (tudo logado depois é "desta sessão"), aplicado no card por exercício e no contador do cabeçalho. Bônus: referência discreta "Última vez: X reps × Ykg" acima do input, só quando existe um registro anterior pro mesmo número de série — sem gráfico nem card extra. Histórico pra gráficos não precisou de nada novo (`SetLog` já guarda tudo pra sempre; `getLoadHistory` já usa isso). Novo e2e dedicado (`segunda-semana-flow.spec.ts`) reproduz 2 ciclos completos (semana 1 → conclui → semana 2) provando a correção ponta a ponta. *Modelo: Sonnet 5. 48/48 Jest/RTL (5 testes novos), 32/32 Playwright (1 novo).*

**Fase 41 — Grupo D: Um Programa Aplicado por Aluno, por Personal.** `workoutProgramsService.apply()` não tinha nenhuma checagem — um Personal podia aplicar quantos programas quisesse ao mesmo aluno. Agora, aplicar um 2º programa ao mesmo aluno retorna 409 ("Este aluno já tem o programa "{nome}" aplicado por você. Exclua-o antes de aplicar um novo."). Escopado por `personalId` de propósito: um aluno pode ter mais de um Personal vinculado (`ClientRelation` já é N:N de verdade, confirmado no schema) — cada Personal só é limitado ao PRÓPRIO programa aplicado, nunca ao de outro profissional (testado explicitamente). Decisão registrada com o fundador: sem substituição automática nesta fase — excluir um programa aplicado já apaga o histórico de séries (SetLog) real do aluno (cascata da Fase 31), então "substituir" ficaria destrutivo demais pra virar um botão único escondendo essa perda; o Personal usa a exclusão que já existe (com o aviso que já existe) e aplica o novo depois. *Modelo: Sonnet 5. 255/255 backend (2 testes novos: bloqueio + caso multi-Personal; precisou de 3 alunos de teste extras e subir o limite freemium só no Personal de teste, pra não colidir com a regra nova), 32/32 Playwright sem regressão.*

**Fase 34 — `origin` (PERSONAL | SELF) em WorkoutProgram + guards de autorização (MASTER_SPEC, Seção 8, Grupo B).** Fundação do pivô B2C: `WorkoutProgram.origin` (enum, default `PERSONAL`, backfill nos registros existentes) distingue um programa PRESCRITO por um Personal de um template `SELF` (curado pelo admin, aplicado direto pelo aluno — Fase 34.5). `personalId` virou nullable em `WorkoutProgram` **e** em `Workout` (achado só na auditoria — o `Workout` tem seu próprio `personalId` independente, não documentado no roadmap original) — `personalId === null` sempre coincide com `origin: SELF`, nunca precisa ser checado separado. Guards de `apply()`/`addSession()`/`deleteProgram()` passam a checar `origin !== "PERSONAL"` como defesa em profundidade (mesmo quando o `personalId` nulo já bastaria por construção). Toda listagem usada pelo fluxo do Personal (`listByPersonal`, `findAppliedProgramForAlunoByPersonal`) filtra `origin: "PERSONAL"` explicitamente. Risco reduzido descoberto na auditoria: como só o ADMIN cria/edita templates SELF (Fase 34.5), o aluno nunca chama os endpoints de mutação do Personal — os guards já bloqueavam por construção (personalId nulo nunca bate), então o footprint real de mudança foi menor que o mapeamento inicial sugeria. *Modelo: Opus 4.8 (item mais sensível de autorização do momento, conforme pedido do fundador). 268/268 backend (5 testes novos: default origin, listByPersonal nunca vaza SELF, apply/addSession/deleteProgram 403 em origin SELF, Workout.personalId nullable confirmado via insert direto).*

**Fase 34.5 — Meu Treino Pessoal: templates curados pelo admin, aplicação pelo aluno (MASTER_SPEC, Seção 8, Grupo B).** Depende da Fase 34. Nova tela `/nimbus/treinos-pessoais`: admin cria/edita templates `WorkoutProgram` com `origin: SELF` (nome + esquema de sessão LETTER/WEEKDAY + exercícios, reaproveitando o mesmo `AddExerciseForm` do fluxo do Personal via um novo prop `addExerciseFn` — evita duplicar ~250 linhas de UI de escolha de exercício). Nova tela `/meu-treino-pessoal` no dashboard do aluno: lista os templates (`GET /api/workout-programs/self-templates`, qualquer usuário autenticado — sem dado sensível ali pra restringir por role no backend, a tela em si é que só é oferecida a ALUNO), aplica = cópia (mesmo padrão da Fase 16: `personalId: null`, `origin: SELF`, `alunoId` preenchido), sem acesso ao catálogo completo nem montagem livre. Mesmo `SetLog`/histórico de evolução dos treinos prescritos pelo Personal, sem aba separada. "Crie seu treino do zero" é placeholder visual ("em breve") sem lógica nenhuma atrás. CTA de upsell pós-treino (só em treinos `origin: SELF`, via prop opcional `upsell` em `PostWorkoutSummaryModal`) ficou só "Convide um Personal" — "assinar plano PRO" foi descartado por não existir nenhum plano pago pro aluno hoje. *Modelo: Sonnet 5. 274/274 backend (8 testes novos em `admin-self-templates.test.ts`), 48/48 Jest/RTL, 33/33 Playwright (novo `meu-treino-pessoal-flow.spec.ts`).*

**Fase 42 — Dashboard do aluno com 2 blocos + convite copiável pro Personal (MASTER_SPEC, Seção 8, Grupo B, item "Fase 36" — renumerado aqui porque "Fase 36" já estava em uso no histórico deste arquivo para outra entrega não relacionada).** Antes da Fase 34.5, "tem programa" e "tem Personal vinculado" eram a mesma coisa — deixou de valer com templates `origin: SELF`. Dashboard agora tem 2 blocos independentes, cada um com sua própria "próxima sessão" (`NextSessionCard` extraído, reaproveitado nos dois): "Prescrito pelo seu Personal" (origin `PERSONAL`) e "Meus treinos" (origin `SELF`, inclui os templates aplicados da Fase 34.5, com atalho pra `/meu-treino-pessoal` quando ainda não aplicou nenhum). "Tem Personal vinculado" corrigido para usar o vínculo real (`GET /api/support/my-personals`, já existente, filtrado por `professionalType: "PERSONAL"`) em vez de inferir de "tem programa" — bug latente que a Fase 34.5 introduziria se não corrigido aqui. Card de convite copiável (mesmo padrão "copiar texto pronto + feedback de copiado" da Fase 12/`VincularAlunoForm`, direção invertida: aluno convidando um Personal) aparece só quando não há nenhum Personal vinculado. Decisão do fundador: o item "Fase 37" do MASTER_SPEC (inverter quem inicia o `ConnectionRequest`) foi **descartado** — esse fluxo aluno→Personal já existe desde a Fase 21 (achado na auditoria); o pedido real era compartilhar o link de instalação do app, jogado pro roadmap futuro junto da publicação nas lojas. *Modelo: Sonnet 5. 48/48 Jest/RTL, 35/35 Playwright (novo `dashboard-blocos-flow.spec.ts`).*

**Fase 43 — Lembrete de pagamento (ClientRelation).** O Personal configura, por vínculo, uma próxima data de cobrança (+ recorrência mensal opcional) direto no hub do aluno (`/personal/alunos/[alunoId]`, novo card). Mecanismo deliberadamente simples por escolha do fundador (não Cloud Scheduler + Terraform): checagem no LOGIN do aluno (`authService.login`, só quando `role === "ALUNO"`) — sem cron/scheduler nenhum, coerente com a ausência total dessa infra no projeto (Cloud Run escala a zero, nenhuma API de scheduler habilitada). Dispara UMA notificação in-app por vínculo vencido via o domínio `notifications` já existente. "Já disparou" não precisa de campo à parte: disparar sempre avança a data (recorrente, +1 mês) ou limpa (não-recorrente) a própria `paymentReminderDueDate` — o mesmo vencimento nunca dispara duas vezes por construção. Zero processamento de pagamento real. *Modelo: Sonnet 5. 284/284 backend (6 testes novos em `relations.test.ts`), 48/48 Jest/RTL, 36/36 Playwright (novo `lembrete-pagamento-flow.spec.ts`, 2 sessões de navegador separadas).*

**Fase 44 — Billing de 3 degraus (Free/Base/Plus).** `PlanoAssinatura` evolui de 2 estados (`FREE/PAGO`) pra 3 (`FREE/BASE/PLUS` — migration com backfill seguro de `PAGO` pra `BASE`, embora zero usuários reais tivessem `PAGO`: Stripe segue 100% inativo em produção, sem `STRIPE_*` configurado). Free: 3 alunos, como hoje. Base: 20 alunos + pode ativar `availableForNewStudents` (diretório de descoberta da Fase 21, antes irrestrito por plano). Plus: alunos ilimitados (sentinela numérico `1_000_000`, mesma checagem `count >= limiteAlunos` de sempre — sem caminho de código à parte pra "ilimitado") + aparece com destaque/prioridade no diretório (`orderBy: [{planoAssinatura: "desc"}, {createdAt: "asc"}]` — ordem de declaração do enum já resolve a prioridade). Webhook do Stripe evoluiu de 2 pra 4 preços (Base/Plus × mensal/anual): degrau detectado via `session.metadata.tier` no `checkout.session.completed` (setado por nós ao criar o Checkout, sem chamada extra à API) e via `price.id` atual da subscription em `customer.subscription.updated` (única fonte confiável quando o cliente troca de degrau pelo Portal do Cliente, fora do nosso Checkout — metadata da criação não se atualiza sozinha nesse caso; price desconhecido cai em BASE por segurança, nunca PLUS por adivinhação). Bug real corrigido: downgrade pra FREE não desligava `availableForNewStudents` — o profissional continuava aparecendo no diretório mesmo sem mais direito a isso; `applyFreePlan` agora desliga. Defesa em profundidade: a busca do diretório também filtra `planoAssinatura: { not: "FREE" }` direto na query, não só no gate de ativação. Valores em R$ são placeholder (o que importa nesta fase é a estrutura de degraus, não o preço final) — `BILLING_SETUP.md` e `.env.example` atualizados pros 4 Price IDs. Novo script `db:grant-plan` (mesmo espírito do `db:seed:admin`: bootstrap de estado sem caminho de self-service via HTTP) usado só por testes E2E que precisam de um Personal Base/Plus sem passar pelo Stripe de verdade. *Modelo: Sonnet 5. 284/284 backend (10 testes novos: upgrade/downgrade por degrau, troca de degrau via price da subscription, price desconhecido não quebra, gate de disponibilidade Free bloqueado + defesa em profundidade + prioridade Plus no diretório), 48/48 Jest/RTL, 38/38 Playwright (novo `billing-tiers-flow.spec.ts`; `descoberta-flow.spec.ts`/`upgrade-flow.spec.ts` ajustados pro novo gate/UI de 3 degraus).*

**Fase 45 — Montagem Inteligente (gerador de PROGRAMA de treino determinístico) + CTA de destaque no dashboard do Personal + simplificação do card de programa.** Cancela e substitui a Fase 39 do MASTER_SPEC ("Sugestão de treino via IA", pesquisa-only) — resolve a mesma necessidade sem provedor/prompt/rate-limit de LLM nenhum. **Correção de escopo no mesmo dia**: a 1ª versão só gerava UMA sessão avulsa (mal-entendido do pedido original); "gerar treino rápido" significa montar o PROGRAMA inteiro — todas as sessões do esquema escolhido. `POST /api/workouts/generate` continua sendo um motor de regras 100% determinístico POR SESSÃO (recebe `muscleGroups`, 1º = principal, + `goal`; devolve um RASCUNHO não-persistido, 3 exercícios pro grupo principal + 2 por secundário, séries/reps/descanso fixos por objetivo — Hipertrofia 3×8-12/60s, Força 4×4-6/120s, Resistência 3×15-20/45s), mas agora o FRONTEND o chama num wizard multi-sessão: (1) setup — nome do programa, esquema (Letras A-E ou Dias da semana) e objetivo, este fixo pra todas as sessões; (2) por sessão, na sequência do esquema — grupos musculares só daquela sessão, gera/revisa/edita (ou "Pular esta sessão", 0 exercícios), e então "Próximo treino →" (avança pra próxima letra/dia) ou "Salvar programa de treinamento" (persiste tudo montado até ali, mesmo parando antes da última sessão — ex: só A e B de um esquema A-E). `level` não filtra o catálogo de forma rígida (curadoria de `difficultyLevel` hoje pouco granular, maioria `INTERMEDIARIO`) — só reordena por preferência. Nada é persistido até o "Salvar" final — reusa os mesmos 3 endpoints já existentes em sequência (`POST /api/workout-programs` → `POST /api/workout-programs/:id/sessions` por sessão montada → `POST /api/workouts/:id/exercises` em loop por exercício), sem endpoint novo de gravação em lote. Dashboard do Personal: "⚡ Gerar Treino Rápido" é o CTA PRINCIPAL (resolve o pedido de tornar a criação de templates mais descobrível); fluxo manual continua via link menor. Card de "Treinos prescritos" simplificado — antes expandia TODAS as sessões de cada programa inline (poluía a tela com vários alunos vinculados); agora mostra só nome do programa + contagem de sessões, clicável pra abrir a tela própria do programa, onde cada dia/letra é editado individualmente (pedido do fundador, mesmo padrão já usado em `/personal/programas`). *Modelo: Sonnet 5. 294/294 backend (10 testes em `workout-generator.test.ts`, incluindo confirmação de que todo `exerciseId` existe no catálogo e nada é persistido pela chamada — sem mudança nesta correção, o motor por-sessão continua igual), 48/48 Jest/RTL, 41/41 Playwright (`montagem-inteligente-flow.spec.ts` reescrito pro fluxo multi-sessão; `dashboard-consolidado-delete-flow.spec.ts` ajustado pro card simplificado).*

**Fase 46 — Internacionalização (PT/EN/ES).** next-intl SEM prefixo de URL (locale resolvido por cookie, não por segmento `[locale]`) — Capacitor carrega a produção ao vivo (`server.url` no `capacitor.config.ts`, não é bundle estático), então SSR/proxy funcionam normalmente dentro do WebView. `proxy.ts` (nome novo do middleware no Next 16) detecta o idioma do dispositivo via Accept-Language na primeira visita e grava um cookie `locale`; sem idioma suportado, cai pro português. Nova tela `/configuracoes` (item novo no menu hambúrguer, ao lado de Perfil/Sair, pensada pra crescer com mais preferências no futuro) com seletor de idioma — persistência dupla: cookie (aplica na hora, mesmo dispositivo) + `User.locale` nullable no banco (sincroniza entre dispositivos; `null` = nunca escolheu, frontend detecta sozinho). Catálogo de exercícios: tabela nova `ExerciseTranslation` (`exerciseId` + `locale` + `name`/`muscleGroup`/`description`) SEM linha em português — português sempre lido direto de `Exercise` (fonte da verdade); fallback por exercício individual (não por catálogo inteiro) quando a tradução daquele idioma ainda não existe pra aquele exercício específico. Header `x-locale` (não vem do JWT — funciona até em telas sem sessão, como `/login`) carrega o idioma ativo do frontend pro backend em toda chamada; `GET /api/exercises`, `GET /api/workouts/:id` e `GET /api/workout-programs/:id` devolvem nome/categoria/descrição já traduzidos. Extração mecânica de texto de UI cobriu a navegação inteira + ~80 arquivos de `app/`/`components/` (~700 chaves de tradução, `frontend/messages/{pt,en,es}.json`) — nenhum conteúdo gerado por usuário (notas de prescrição, anamnese, mensagens de dúvida) foi tocado, só texto de interface e o catálogo de exercícios. Catálogo de ~171 exercícios totalmente traduzido pra EN/ES (nome + categoria + descrição), terminologia natural de academia (não tradução literal — ex: "Levantamento Terra" → "Deadlift"/"Peso Muerto", não "Ground Lifting"); 3 exercícios sem nome-padrão 100% consolidado entre academias foram sinalizados pro fundador revisar (`Remada Serrote no Cabo`, `Rosca Cross na Polia`, `Abdominal Remador`) em vez de adivinhados. Achado durante a extração: o Chrome empacotado deste ambiente não abre em português por padrão, o que expôs que boa parte da suíte Playwright nunca fixava o locale do navegador — sem problema enquanto nada era traduzido de verdade, mas passou a fazer specs não relacionados renderizarem em inglês assim que o header/nav virou texto real; corrigido com um único `locale: "pt-BR"` default em `playwright.config.ts` (specs que testam outros idiomas já sobrescrevem explicitamente). *Modelo: Sonnet 5. 308/308 backend (novo `exercise-translation.test.ts` + testes do endpoint de locale — a suíte completa só é estável com `--runInBand`, contenção de conexão pré-existente do Postgres local ao rodar em paralelo, não uma regressão desta fase), 48/48 Jest/RTL, 44/44 Playwright.*

**Fase 47 — Triagem + correções de performance (backend Prisma/Postgres + frontend TanStack Query).** Precedida por uma triagem (2 agentes de pesquisa em paralelo, sem código) que produziu 25 candidatos priorizados em `MASTER_SPEC.md` (Grupo D). Todos os itens de alto impacto implementados nesta fase, mais os de médio/baixo esforço; adiado por decisão explícita apenas o que exigiria endpoint novo (contrato de API maior). **N+1 corrigidos** (1 query batelada no lugar de 1-por-item): `relations.service.listRelations` (lista de alunos do Personal), `workout-summary.service.buildPersonalRecords` (detecção de PR ao concluir treino), `relations.service.checkAndFireDueReminders` (roda em todo login de aluno), `workout-programs.service.getProgram` (tradução EN/ES batelada por programa em vez de 1 query por sessão). **Histórico de `SetLog` limitado** a 100 séries mais recentes por exercício prescrito (`workout-programs.repository.findProgramWithSessions`, `workouts.repository.findByIdWithExercises`) — antes crescia sem teto pra sempre; teto generoso o bastante pra não mudar nenhum comportamento visível (`frontend/lib/utils.ts::splitSetLogsBySessionBoundary` só usa o ciclo anterior mais recente pra referência "última vez"). **`select` explícito** no catálogo público de exercícios (`exercises.repository.findAll`, mantém `description` — usado de verdade e coberto por teste de contrato HTTP em `exercise-translation.test.ts` — mas corta `createdAt`/`updatedAt`, nunca lidos) e na checagem de nome parecido do admin (nova `adminRepository.listAllExerciseNames`, `select: {id, name}`, separada da listagem completa usada pela tabela do admin). **`updateUserRole`** ganhou `findUserRoleById` dedicado (`select: {id, role}`) em vez de carregar o `User` inteiro. **Frontend**: `staleTime` global de 30s no `QueryClient` (antes `0` implícito — todo remount refazia fetch) + override de 5min no catálogo de exercícios (`add-exercise-form.tsx`/`generate-workout-modal.tsx`); hub do aluno (`/personal/alunos/[alunoId]`) parou de esperar a lista inteira de alunos resolver pra só então disparar as outras 4 queries em série (elas só precisam do `alunoId` do `useParams`, a posse já é validada no backend via `ClientRelation` independente de quando o client dispara a chamada); lista de ~170 exercícios do admin (`/nimbus/exercicios`) ganhou `React.memo` por linha (editar 1 não re-renderiza as outras 169); `useMemo` adicionado nos `.filter`/`.sort`/`.find`/`Map` recalculados sem necessidade em `dashboard`, `sessoes/[sessionId]`, `treinos/[id]` e `profissionais`. **Adiado de propósito**: endpoint dedicado "1 sessão"/"1 vínculo" pra eliminar de vez o over-fetch de programa inteiro/lista inteira de alunos em 2 telas — mudança de contrato de API, não uma otimização de query; e revisão de `progress.service` (janelas de `SetLog` sobrepostas entre `getWeeklySummary`/`getFrequency`) — hoje não chamadas juntas na mesma request, sem bug real a corrigir ainda. Migration puramente aditiva (só `CREATE INDEX`, nenhuma alteração de dado) cobrindo `Workout`, `WorkoutProgram`, `SetLog`, `ConnectionRequest`, `Notification`, `SupportThread`, `ClientRelation` e `users` (diretório de descoberta) nos campos mais filtrados que não tinham índice. 2 agentes em paralelo (backend mecânico + frontend mecânico), arquivos sem sobreposição entre si e com o trabalho já feito manualmente antes. *Modelo: Sonnet 5. 308/308 backend (1 falha intermitente pré-existente em `setlogs.test.ts` — passa isolado e num 2º full-run, não reproduzida por nenhuma mudança desta fase), 48/48 Jest/RTL, 44/44 Playwright.*

**Fase 48 — Camada de documentação pra agentes de IA (`AGENTS.md` raiz + por domínio + diagramas Mermaid) + correção de layout no dashboard do Personal.** Nova camada, curta e objetiva de propósito (economizar tokens de contexto em tarefas futuras), separada do `MASTER_SPEC.md` (decisões/arquitetura) e do `STATUS.md` (histórico): `AGENTS.md` na raiz (stack+versões exatas, comandos, convenções, índice de domínios) + um `src/<domínio>/AGENTS.md` por domínio (10 no total — todos sob 200 linhas), cada um com propósito, entidades principais, regras de autorização, pontos de cuidado e estado atual (sem histórico de fases). 10 agentes em paralelo, um por domínio, cada um lendo o próprio código real (services/controllers/schema) em vez de assumir a partir de documentação existente — encontraram e documentaram, entre outras coisas: `billing` não tem tabela própria (todo estado mora em `User`, comentário do schema dizendo "não implementado" está desatualizado); `admin` é o único domínio sem checagem de `ClientRelation` por design; `progress` duplica a lógica de streak com `workout-summary.service.ts` sem helper compartilhado; `connections` mantém o pedido `PENDENTE` (não falha) se o aceite esbarrar no limite Freemium. Três diagramas Mermaid versionados em `docs/` (não imagem estática): `architecture.mermaid` (relação domínio-a-domínio, não FK de tabela — prioriza legibilidade, omite a dependência universal de todo domínio em `auth`/`User`) + dois complementares (`auth-flow.mermaid`, `billing-flow.mermaid`) pensados tanto pra um agente consultar sob demanda quanto pro fundador colar direto numa conversa nova sem reexplicar a arquitetura. Redundância com `MASTER_SPEC.md` (Seções 2.1/2.2/2.3 — stack/estrutura de pastas/modelo de dados essencial) identificada e reportada ao fundador pra decisão própria sobre o que cortar; nada removido de lá nesta fase. **Correção incidental** (relatada pelo fundador, não achada pela documentação): no dashboard do Personal, tanto a lista "Alunos vinculados" quanto os cards de "Treinos prescritos" empurravam o botão de ação (Gerenciar/Abrir) pra fora do card quando o e-mail do aluno era longo — `flex justify-between` sem `min-w-0`/truncamento no texto flexível. Corrigido com `min-w-0 flex-1` + `break-all` (e-mail, mesmo padrão já usado no hub do aluno) ou `truncate` (nome de programa) no bloco de texto, e `shrink-0` no grupo de ações. *Modelo: Sonnet 5. Sem mudança de schema/endpoint — só documentação + CSS; `tsc --noEmit` do frontend limpo.*

**Fase 49 — Performance, rodada 2 (triagem mais profunda que a Fase 47) — implementados os 2 itens de maior impacto/velocidade + tarefas agregáveis.** Segunda triagem (4 agentes de pesquisa em paralelo: cache/HTTP, escala de query com volume, waterfalls de frontend, bundle/carregamento inicial) deliberadamente mais funda que a Fase 47 — foi atrás do que só dói com VOLUME (tabelas sem índice que só aparecem depois de milhares de linhas) e de padrões repetidos entre domínios, sem repetir N+1/índice/payload óbvio já corrigido. Produziu ~40 achados priorizados, documentados em `MASTER_SPEC.md` (Grupo E). Implementado nesta fase (migration manual + 3 agentes em paralelo sobre arquivos sem sobreposição): **migration aditiva de índices** em 5 tabelas que ficaram de fora da 1ª triagem (`WorkoutExercise(workoutId/exerciseId)`, `LoginLog(createdAt)`, `AdminAccessLog(createdAt)`, `AdminAuditLog(createdAt)`, `SupportMessage(threadId)`, `users(createdAt)`, `SupportThread(status,createdAt)`); **detecção de PR** (`workout-summary.repository.ts`, chamada a cada série logada — o caminho de escrita mais quente do app) movida de "trazer todo o histórico e tirar `Math.max` em JS" pra agregação `MAX` no Postgres (versão batelada usa `$queryRaw` com `GROUP BY` via join, já que Prisma `groupBy` não agrupa por campo de relação); **`getLoadHistory`** (domínio progress, gráfico de evolução) mesma mudança, com verificação explícita de que `SetLog.loggedAt` é `timestamp` sem timezone (sem risco de divergência no truncamento por dia); **`GET /api/setlogs`** ganhou o mesmo cap de 100 que os endpoints irmãos já tinham; **cache em memória do catálogo de exercícios** (~171, TTL 5min espelhando o `staleTime` do frontend, invalidado pelo CRUD do admin) + o mesmo cache pras traduções EN/ES, + `ETag`/304 em `GET /api/exercises` — resolveu de graça o problema da Montagem Inteligente (1 query por grupo muscular virou cache-hit). Achado real durante a implementação: um teste (`exercise-translation.test.ts`) escrevia tradução direto via Prisma contornando o admin e dependia disso não ficar em cache — corrigido chamando `invalidateCache()` no próprio teste. O restante dos ~40 achados (frontend: invalidação de query cache em cascata, `staleTime` subótimo, `html-to-image`/`recharts` embarcados eager, bundle de mensagens i18n inteiro, waterfall do dashboard do aluno; backend: paginação de listas, response schemas do Fastify, dedup de query em `addSession` **[AUTHZ]**, `admin.getOverview` **[BILLING]**) ficou documentado por tarefa em `MASTER_SPEC.md` (Grupo E, itens 31-43), não implementado nesta fase. *Modelo: Sonnet 5 (índices) + 3 agentes em paralelo (Sonnet 5). 309/309 backend, `tsc --noEmit` limpo (backend e frontend não foi tocado nesta fase).*

**Fase 50 — Categoria "treino em casa" + subdivisão de "Pernas" em 5 grupos musculares.** Duas mudanças de catálogo curadas via pesquisa real no YouTube (4 agentes em paralelo, todo `mediaUrl` verificado por fetch/oEmbed real da página antes de entrar no catálogo — nenhuma URL gerada/adivinhada). **(1) Treino em casa**: 42 exercícios novos cobrindo todos os grupos musculares, usando só peso corporal ou itens domésticos (mochila, toalha, cadeira, parede, degrau) — sem tabela/campo novo, reaproveita o `equipment` (texto livre) já existente com um valor a mais (`"Itens Domésticos"`) ao lado do já existente `"Peso Corporal"`. **(2) "Pernas" dividido em 5 grupos** — `Quadríceps`, `Glúteos`, `Posterior da Coxa`, `Panturrilhas`, `Adutores e Abdutores` — reclassificação manual dos 31 exercícios existentes pela ênfase muscular real (agachamentos/afundos → Quadríceps; stiff/levantamento terra → Posterior da Coxa; elevação pélvica → Glúteos; cadeira adutora/abdutora → Adutores e Abdutores) + rodada extra de curadoria (bodyweight E equipamento de academia) mirada nos 2 grupos que ficariam finos demais (`Glúteos`/`Adutores e Abdutores`, só 2 exercícios cada antes, foram a 10 e 12). `muscleGroup` é string livre no schema (não enum) — os seletores do admin e da Montagem Inteligente já derivam a lista de grupos ao vivo do banco (`DISTINCT muscleGroup`), então a subdivisão inteira foi só dado, zero mudança de código/schema/migration. Script novo (`prisma/seed-treino-em-casa-e-pernas.ts`, mesmo padrão idempotente de `seed-featured-exercises.ts`) roda a reclassificação + criação; `data/exercises_seed.json` (usado por `db:seed`) atualizado junto pra um ambiente novo já nascer com o catálogo completo, e o antigo `seed-featured-exercises.ts` teve sua reclassificação obsoleta ("Levantamento Terra Romeno" → Pernas, da Fase 34) removida — o exercício já nasce direto como "Posterior da Coxa" agora. Catálogo: 171 → 213 exercícios. Achado ao vetar os 4 agentes: dois deles (curadoria em casa e curadoria de academia pra Glúteos) usaram por engano o MESMO vídeo do YouTube pra dois exercícios diferentes ("Elevação Pélvica Unilateral" peso-corporal vs. com halter) — descartada a versão com halter em vez de arriscar um vídeo incompatível com o exercício. *Modelo: Sonnet 5, com 4 agentes de pesquisa em paralelo pra curadoria (WebSearch + WebFetch, cada um verificando as próprias URLs antes de propor). 309/309 backend (2 ajustes: contagem de catálogo nos testes agora deriva do JSON atualizado; um `muscleGroup` de teste de "Pernas" pra "Quadríceps"), `tsc --noEmit` limpo. Sem migration — puramente dado.*

**Fase 51 — 3 grupos musculares novos: Antebraço, Trapézio, Flexores do Quadril.** Mesmo padrão da Fase 50 (curadoria real via YouTube, 2 agentes em paralelo, todo `mediaUrl` verificado por oEmbed/fetch real da página antes de entrar no catálogo). Reclassificação de 6 exercícios já cadastrados que se encaixavam melhor num grupo novo: `Rosca Punho` e `Rosca Inversa com Barra` (Bíceps → Antebraço, ênfase real é flexores/extensores de punho e braquiorradial); `Encolhimento com Barra`/`com Halteres` (Costas → Trapézio) e `Encolhimento no Cabo` (Ombro → Trapézio, o exercício clássico de trapézio finalmente com grupo próprio); `Elevação em Y no Banco Inclinado` (Ombro → Trapézio — a própria descrição já cadastrada deste exercício mencionava "contraindo o trapézio inferior", inconsistência resolvida). Achado ao vetar os 2 agentes: um deles propôs recriar esse mesmo exercício de Elevação em Y com um nome ligeiramente diferente ("Trapézio Inferior") em vez de reconhecer que já existia — descartada a proposta duplicada, mantida só a reclassificação do original. 13 exercícios novos criados: 7 em Antebraço (rosca de punho direta/invertida, farmer's walk, pinça de anilha, wrist roller, dead hang na toalha, rosca inversa no cabo), 3 em Trapézio (encolhimento atrás das costas, rack pull, remada alta pegada de arranco), e **Flexores do Quadril nasceu do zero** (0 exercícios antes) com 9 exercícios cobrindo cabo, máquina, peso corporal e itens domésticos — cuidado extra na curadoria pra não duplicar os exercícios de abdômen já existentes que também envolvem flexão de quadril (elevação de pernas/joelhos suspensos), buscando enquadramento e nome claramente distintos. `muscleGroup` continua string livre no schema — zero mudança de código/schema, mesma razão da Fase 50. `data/exercises_seed.json` e `seed-featured-exercises.ts` atualizados junto. Catálogo: 213 → 232 exercícios. *Modelo: Sonnet 5, 2 agentes de pesquisa em paralelo pra curadoria. 309/309 backend, `tsc --noEmit` limpo. Sem migration — puramente dado.*

**Fase 51.1 — Correção real: "treino em casa" era só dado, não aparecia em lugar nenhum da interface.** Achado do fundador após a Fase 50: a categoria inteira (equipment `"Peso Corporal"`/`"Itens Domésticos"`) existia só no banco — nenhuma tela deixava o Personal filtrar/enxergar isso, então a curadoria toda ficava invisível, misturada no catálogo geral. Corrigido com um chip de filtro novo ("🏠 Treino em casa") em `AddExerciseForm` (o seletor de exercício reaproveitado tanto na prescrição normal do Personal quanto na curadoria de templates SELF do admin, `/nimbus/treinos-pessoais` — corrige os dois lugares de uma vez), combinável com o filtro de grupo muscular já existente (ex: "Peito" + "treino em casa" juntos), puramente client-side sobre o catálogo já carregado. Fora de escopo por ora: a Montagem Inteligente (gerador determinístico) ainda não tem essa opção — geraria a mesma necessidade só por grupo muscular, sem filtrar por equipment; exigiria passar o filtro até `workout-generator.service.ts` no backend, registrado como próximo passo natural se o fundador quiser. *Modelo: Sonnet 5. Frontend puro (`tsc --noEmit` + 48/48 Jest/RTL), sem mudança de backend/schema.*

**Fase 52 — "Meu Treino Pessoal": carrosséis "Treino em Casa"/"Treinos Premium" + banner + "1 treino ativo por vez".** `WorkoutProgram` ganhou `category` (`GERAL`/`HOME`/`PREMIUM`, migration aditiva) e `bannerImageUrl`, ambos só relevantes em templates `origin: SELF`. `/meu-treino-pessoal` passa a agrupar por categoria: a lista plana `GERAL` de sempre (inalterada) + dois carrosséis novos, swipeable via CSS scroll-snap nativo (zero libs — este projeto deliberadamente não tem embla/swiper) com navegação por "bolinha" (`IntersectionObserver` rastreando o slide ativo). **"Treino em Casa"**: totalmente funcional, aplica de verdade. **"Treinos Premium"**: cadeado 🔒 decorativo em todo slide — decisão confirmada com o fundador (não existe conceito de aluno pagante ainda), clique só mostra "em breve" sem chamar a API. Banner (16:9, 1200×675, recorte central + compressão client-side no canvas, mesmo espírito do avatar) sobe pelo admin em `/nimbus/treinos-pessoais` (novo componente `TemplateBannerUpload`, mesmo bucket GCS de mídia de exercício, pasta `banners/` separada); sem banner, o slide vira um card estático só com o nome, dentro do mesmo carrossel. **Bug real corrigido a caminho** (achado ao investigar antes de implementar): não existia NENHUMA trava de "1 treino pessoal ativo por vez" — um aluno podia aplicar o mesmo template (ou vários) repetidamente sem aviso algum, diferente do fluxo do Personal (que já tinha o equivalente por-Personal desde a Fase 41). Implementada do zero: aplicar um 2º template sem confirmar devolve 409 (`code: SELF_PROGRAM_EXISTS` + nome/id do programa atual); o frontend abre um diálogo de confirmação (`ReplaceSelfTemplateDialog`, mesmo padrão zero-dependência de overlay já usado no card pós-treino) e reenvia com `replace: true` — o backend apaga o anterior (cascata) e aplica o novo na mesma chamada. Execução: schema + backend feitos manualmente (a invariante de "1 ativo" mexe em fluxo de aplicação já existente, mereceu atenção redobrada) + 2 agentes em paralelo pro frontend (admin e aluno) — tipos/API client (`WorkoutProgram.category`/`bannerImageUrl`, `ApiError.data`, `applySelfTemplate(id, replace?)`) atualizados manualmente antes de disparar os agentes, pra evitar os dois colidirem no mesmo arquivo compartilhado. *Modelo: Sonnet 5. 319/319 backend (18 testes novos: categoria, banner — válido/formato errado/remoção/autorização —, e a invariante de substituição), 48/48 Jest/RTL, `tsc --noEmit` limpo nos dois lados.*

**Fase 53 — 3 programas curados de "Treino em Casa" cadastrados (categoria HOME) + correção de tradução.** Conteúdo pedido pelo fundador, com sessões/exercícios/séries/reps exatos: **"Hipertrofia & Força em Casa"** (A: Peito/Ombro/Tríceps, B: Membros Inferiores, C: Costas/Bíceps/Antebraço/Core), **"Seca Barriga em Casa"** (A: Full Body Burn & Cardio, B: Core Extremo e Definição, C: Agilidade e Resistência), **"Bumbum na Lua, Pernas & Core em Casa"** (A: Glúteos e Isquiotibiais, B: Coxas/Adutores/Abdutores, C: Core/Quadril/Estabilidade Postural) — 9 sessões, 46 linhas de exercício ao todo, todos os 42 nomes de exercício únicos verificados contra o catálogo real antes de escrever o seed (zero "não encontrado"). `restSeconds` (não especificado pelo fundador) escolhido por categoria — 60s hipertrofia/força, 30s metabólico/cardio, 45s isolamento/glúteos, mesmo espírito dos presets da Montagem Inteligente; qualificadores "por perna"/"por lado" viraram `notes` (mesmo uso já dado a esse campo em todo o catálogo). **Achado real ao preparar tradução**: exercícios reclassificados nas Fases 50/51 (`Rosca Punho`→Antebraço, `Encolhimento com Barra`→Trapézio, etc.) mantinham o `muscleGroup` traduzido EN/ES **desatualizado** — ex: um exercício já "Antebraço" no PT ainda aparecia como "Biceps" pra quem usa o app em inglês, porque a reclassificação da Fase 50/51 só tocou o campo canônico PT, nunca `ExerciseTranslation`. Corrigido em 37 exercícios (74 linhas EN+ES) — bug que afetava o catálogo inteiro, não só os 3 programas novos. Além disso, os 26 exercícios (dos 42 usados) que nasceram nas Fases 50-52 sem tradução nenhuma ganharam EN/ES completos (nome + grupo muscular + descrição, terminologia natural de academia, mesmo critério da Fase 46). **Pendência sinalizada ao fundador, não resolvida nesta fase**: nomes de programa/sessão (`WorkoutProgram.name`/`Workout.name`) não têm NENHUM mecanismo de tradução no schema hoje (diferente de `Exercise`, que tem `ExerciseTranslation`) — um aluno em EN/ES vê "Hipertrofia & Força em Casa" em português mesmo assim. Construir isso é uma decisão de escopo à parte (tabela nova? só pra SELF? aplicar em todo `WorkoutProgram`?), não assumida silenciosamente. *Modelo: Sonnet 5. Sem migration — puramente dado (2 scripts novos versionados em `prisma/`). 319/319 backend, seed idêntico em dev e produção (verificado campo a campo antes e depois em ambos).*

**Fase 53.1 — 3 correções reais no upload de banner de template SELF, achadas pelo fundador usando a feature.** (1) O upload fazia center-crop tipo "cover" pra encaixar em 16:9, cortando as bordas de qualquer foto que não fosse já exatamente 16:9 — reportado como "não mostra completamente a imagem". Corrigido pra desenhar a imagem inteira em "contain" (nunca corta nada) centralizada sobre um fundo desfocado/escurecido da mesma imagem preenchendo o quadro (mesma técnica de capa do Instagram Stories/YouTube). (2) Ferramentas de geração de imagem raramente batem o pixel exato pedido (um "1200x675" gerado por IA costuma sair com poucos pixels de diferença) — sem tolerância, esse desvio mínimo já disparava o "contain" e deixava uma faixa fina visível no topo/base mesmo pedindo exatamente 16:9. Adicionada tolerância de ~3%: dentro dela, usa "cover" simples (preenche o quadro inteiro, corte imperceptível); só uma proporção genuinamente diferente (retrato, quadrada) usa o contain com fundo desfocado. (3) O banner (quando já vem com o nome estilizado embutido na própria imagem, como o fundador estava gerando) tinha o nome do template desenhado de novo por cima, duplicado, com gradiente de legibilidade — removido o overlay de texto/gradiente quando há banner (o card sem banner continua mostrando o nome normalmente). Os 3 banners já enviados em produção antes dessas correções precisaram ser reenviados pelo admin. *Modelo: Sonnet 5. Frontend puro, `tsc --noEmit` + 48/48 Jest/RTL limpos, sem mudança de backend/schema.*

**Fase 54 — Nova categoria "Treinos Prontos" (academia, grátis) substitui o placeholder "Crie seu treino do zero" + 3 programas cadastrados.** `SelfTemplateCategory` ganhou um 4º valor, `PRONTOS` (migration aditiva, `ALTER TYPE ... ADD VALUE`) — mesmo padrão de carrossel/banner já usado por `HOME`, mas sem cadeado (gratuito, totalmente funcional desde já). O antigo card estático "Crie seu treino do zero" (inerte desde a Fase 34.5, nunca decidido se virava feature paga) foi **removido** de `/meu-treino-pessoal`, com o carrossel novo entrando exatamente no lugar dele. A tela de admin (`/nimbus/treinos-pessoais`) **não precisou de tela nova** — já era genérica por categoria desde a Fase 52 (um seletor cobrindo Geral/Casa/Premium), só ganhou a 4ª opção no dropdown. 3 programas de academia cadastrados (categoria `PRONTOS`), conteúdo exato pedido pelo fundador — foco em aparelhos/polias/halteres/barra, nível iniciante/intermediário: **"Glúteos & Coxas Definitivo (ABC - Feminino)"**, **"Corpo Esculpido & Tônus (ABC - Feminino)"** (3 sessões cada) e **"Shape V: Hipertrofia (ABCD - Masculino/Geral)"** (4 sessões) — 10 sessões, 61 linhas de exercício, todos os 44 nomes únicos verificados contra o catálogo antes do seed (zero "não encontrado"). `restSeconds` (não especificado) seguiu convenção de academia: 90s no primeiro composto mais pesado de cada sessão (geralmente 4 séries), 60s pro restante, 45s em isolamentos leves/isométricos de core. *Modelo: Sonnet 5. 319/319 backend, 48/48 Jest/RTL, `tsc --noEmit` limpo nos dois lados. Seed idêntico em dev e produção (verificado antes/depois nos dois).*

## Progresso Geral das Fases
- [x] Fase 1: Fundação Core, Auth e Estrutura Modular
- [x] Fase 2: Vínculo Personal↔Aluno e Limite Freemium
- [x] Fase 3: Catálogo de Exercícios (AI Seeder) e Prescrição de Treinos
- [x] Fase 4: Execução do Treino e Registro de Carga
- [x] Fase 5: Frontend Web Mobile-First
- [x] Fase 5.5: Hardening Pré-Produção (cookie httpOnly, config de ambiente, script de dev)
- [x] Fase 6: Painel do Personal Trainer (UI)
- [x] Fase 7: Suíte de Testes Automatizados de Frontend
- [x] Fase 8: Evolução (Histórico de Carga e Frequência)
- [x] Fase 9: Consolidação (Polish + Stub de Billing + Preparação Mobile)
- [x] Fase 10: Anamnese + Dúvidas + Notificações
- [x] Fase 11: Módulo de Nutrição & Multi-Profissional
- [x] Fase 12: Polish Visual & Usabilidade
- [x] Fase 13: Deploy em Produção (GCP + Terraform + Neon)
- [x] Fase 14: Painel Administrativo
- [x] Fase 15: Catálogo de Exercícios Expandido
- [x] Fase 16: Programas de Treino (Templates + Múltiplas Sessões + Progresso)
- [x] Fase 17: Consolidação de Pendências (UI + Segurança + Extensão de Domínio)
- [x] Fase 18: UX de Autenticação + Remoção Temporária do Nutricionista da UI
- [x] Fase 19: Spike Capacitor + Auth por Cookie (integrado; teste manual Android pendente)
- [x] Fase 20: Billing Real (Stripe Checkout) (integrado/deployado; env STRIPE_* pendente para ativar)
- [x] Fase 21: Descoberta de Profissionais + Reavaliação de UI
- [x] Fase 22: Reconciliação de Branches (tudo em dev + produção)
- [x] Fase 24: Fluxo de Auth Unificado (Parte 1: backend + Parte 2: frontend)
- [x] Fase 25: Correção do Fluxo Programa→Sessões + Ajustes de Mídia
- [x] Fase 26: Esquema de Sessões (Letras/Dias) + Tela Dedicada por Sessão
- [x] Fase 27: Feedback de Exercício Adicionado + Observações por Prescrição (sem suíte de testes — pendente pra próxima fase)
- [x] Fase 28: Polish do Formulário de Exercício (bug de posição travada, popup, reordenar)
- [x] Fase 29: Hub de Administração do Aluno (programas + evolução + anamnese)
- [x] Fase 30: Foto de Perfil (aluno e Personal, armazenada no banco)
- [x] Fase 31: Consolidação (Dashboard Agrupado + Exclusão de Programas/Templates + Correção do Avatar)
- [x] Fase 32: Infraestrutura de Mídia de Exercícios (bucket GCS + player enquadrado)
- [x] Fase 33: Admin — CRUD do Catálogo de Exercícios (tela dedicada) + Edição de Role
- [x] Fase 33.3: Curadoria do Catálogo (destaque por grupo + Alongamento/Cardio)
- [x] Fase 33.4: Redesign do Dashboard do Aluno (hero contextual + voltagem semanal)
- [x] Fase 34: `origin` (PERSONAL | SELF) em WorkoutProgram + guards de autorização
- [x] Fase 34.5: Meu Treino Pessoal (templates curados pelo admin, aplicação pelo aluno)
- [x] Fase 35: Card de Resumo Pós-Treino (motivação in-app + exportável pro Instagram Stories)
- [x] Fase 36: PR em Tempo Real + Comparação de Volume (fundação de dado, sem tela nova)
- [x] Fase 37: Compartilhamento Nativo no Instagram + Redesign do Card Pós-Treino
- [x] Fase 38: Campo de Reps Limitado a 2 Dígitos + Avanço Automático de Foco
- [x] Fase 39: Grupos A e B pós-lançamento (sugestão WEEKDAY, séries/semana, card pós-treino, cadastro mínimo de nome)
- [x] Fase 40: Grupo C — bug real de corte de sessão no registro de série + referência da última vez
- [x] Fase 41: Grupo D — um programa aplicado por aluno, por Personal
- [x] Fase 42: Dashboard do aluno com 2 blocos + convite copiável pro Personal
- [x] Fase 43: Lembrete de pagamento (ClientRelation)
- [x] Fase 44: Billing de 3 degraus (Free/Base/Plus)
- [x] Fase 45: Montagem Inteligente (gerador determinístico) + CTA de destaque no dashboard do Personal
- [x] Fase 46: Internacionalização (PT/EN/ES) — next-intl, seletor em /configuracoes, catálogo de exercícios traduzido
- [x] Fase 47: Triagem + correções de performance (N+1, índices, staleTime, waterfalls) — itens de alto/médio/baixo impacto do Grupo D do MASTER_SPEC
- [x] Fase 48: Camada de documentação pra agentes de IA (AGENTS.md raiz + por domínio + diagramas Mermaid) + correção de layout no dashboard do Personal
- [x] Fase 49: Performance rodada 2 — índices restantes (5 tabelas), agregação SQL (PR/load-history), cache em memória do catálogo de exercícios + ETag
- [x] Fase 50: Categoria "treino em casa" (42 exercícios) + subdivisão de "Pernas" em 5 grupos musculares (curadoria via YouTube, catálogo 171 → 213)
- [x] Fase 51: 3 grupos musculares novos — Antebraço, Trapézio, Flexores do Quadril (curadoria via YouTube, catálogo 213 → 232)
- [x] Fase 52: Meu Treino Pessoal — carrosséis Treino em Casa/Treinos Premium + banner + "1 treino ativo por vez"
- [x] Fase 53: 3 programas curados de Treino em Casa cadastrados + correção de tradução (muscleGroup desatualizado pós Fase 50/51)
- [x] Fase 53.1: 3 correções no upload de banner (corte indevido, faixa por desvio de proporção, texto duplicado)
- [x] Fase 54: Categoria "Treinos Prontos" (academia, grátis) substitui "Crie seu treino do zero" + 3 programas cadastrados
- [x] Fase 55: i18n do nome de programa/sessão (`WorkoutProgramTranslation`/`WorkoutTranslation`) + banner copiado na aplicação do template + overlay de texto padronizado nos banners + banner substitui o card de sugestão no dashboard ("Meus treinos")
- [x] Fase 55.1: overlay de banner com fonte grande demais — reduzida e alinhamento à esquerda reforçado
- [x] Fase 55.2: tela de admin (`/nimbus/treinos-pessoais`) ganha edição do nome PT + tradução EN/ES do template e de cada sessão (antes só existia via seed script)
- [x] Fase 56: Aluno Premium — guardrails (`AlunoPremiumStatus`, teste grátis de 7 dias uma vez por conta, gate real de `402 PREMIUM_REQUIRED` ao aplicar template PREMIUM — antes era só cadeado decorativo). Preço R$9,90/mês + 30% off no trimestral são constantes documentadas; checkout/Stripe real da assinatura fica pra quando entrar em produção
- [x] Fase 57: 10 programas "Treinos Premium" cadastrados (5 femininos + 5 masculinos, conteúdo avançado do fundador — 40 sessões, técnicas de intensidade em `notes`) + tradução EN/ES completa + remoção da frase solta "Nenhum treino pessoal disponível ainda" (seção GERAL, sem conteúdo real, some por completo quando vazia em vez de mostrar texto sem contexto)
- [x] Fase 58: tela de admin (`/nimbus/usuarios`) ganha concessão/revogação manual de Premium por usuário (`PUT /api/admin/users/:id/premium`) — ALUNO vira `alunoPremiumStatus: ACTIVE` (mesmo entitlement do teste grátis, Fase 56), PERSONAL/NUTRICIONISTA vira `planoAssinatura: PLUS`; exceção documentada à regra de "só o webhook escreve plano", auditada em `AdminAuditLog`
- [x] Fase 59: `WorkoutProgram.description` ("Foco", nullable) — mostrado em `/programas/:id` abaixo do nome, editável em `/nimbus/treinos-pessoais` com tradução EN/ES; backfill dos 10 templates Premium (Fase 57) com o texto original; preview de sessões (nome + descrição + lista, sem aplicar) antes de aplicar um template dos carrosséis "Treino em Casa"/"Treinos Prontos"/"Treinos Premium" — antes o clique já perguntava/aplicava direto
- [x] Fase 60: carrossel "Treinos Premium" passa a vir primeiro em `/meu-treino-pessoal` (antes vinha por último); mais 3 programas Premium focados em emagrecimento/EPOC cadastrados (Queima Fatal 360, Metabolic Shred Pro, Corpo Trincado Extreme — 12 sessões, técnicas de bi-set/tri-set metabólico em `notes`) + tradução EN/ES completa
- [x] Fase 61: card de "próxima sessão" no dashboard do aluno — nome do programa vira link pra `/programas/:id` (mesmo padrão de link com seta já usado na tela), letra do treino em destaque grande + nome da sessão do dia em fonte menor/tom de branco levemente mais escuro (era "letra — nome do programa" duplicando a letra), layout responsivo; label do bloco "Meus treinos" renomeado para "Meus Treinos Pessoais" (EN/ES também ajustados)
- [x] Fase 62: dashboard do Personal reestruturado — lista de alunos vinculados vira link pra nova tela "Gerenciar alunos" (com selo de "sem treino aplicado" por aluno); "Treinos prescritos" vira "Templates de treino", sem mais listar instâncias soltas (cada uma só se vê no hub do próprio aluno agora). Fechada lacuna real: `apply()` agora exige um TEMPLATE (403 se a origem for uma instância já aplicada a outro aluno) — a única forma de reaproveitar o treino de um aluno pra outro é o novo botão "Salvar como template" na tela do programa. `/personal/programas` ganha 3 seções — Meus Templates / Templates Básico (novo `origin: PERSONAL_CATALOG`, curado na mesma tela `/nimbus/treinos-pessoais`) / Templates Premium (reaproveita os 13 templates `SELF`/`PREMIUM` já vendidos ao aluno, sem duplicar conteúdo — exige plano Plus do Personal, 402 `PREMIUM_TEMPLATE_REQUIRED` sem ele)
- [x] Fase 63: filtro rápido por chip (Todos/Feminino/Hipertrofia/Definição/Express) no carrossel "Treinos Premium" de `/meu-treino-pessoal` — hoje com muitos banners, prejudicando a navegação. Novo campo `WorkoutProgram.tags` (`WorkoutTag[]`, múltiplas tags por template), editável só em templates `origin: SELF` na tela de admin (`/nimbus/treinos-pessoais`, tanto na criação quanto na edição de um template já existente) via `PUT /api/admin/self-templates/:id/tags`
- [x] Fase 64: clicar num template Premium bloqueado em `/personal/programas` (Personal sem plano Plus) redireciona direto pra `/personal/upgrade?from=templates`, em vez de abrir o preview normalmente — mesmo espírito do cadeado do aluno gratuito (gate no clique, não só decorativo). Tela de upgrade ganha um aviso específico ("Templates Premium são exclusivos do plano Plus...") quando chega por esse link + novo bullet no card do plano Plus listando o acesso aos templates
- [x] Fase 65: (1) Personal agora pode excluir um exercício já prescrito (`DELETE /api/workouts/:id/exercises/:exerciseId`, antes só dava pra adicionar/reordenar), com confirmação inline; (2) novo botão "Ver como o aluno vê" na tela de sessão do Personal, abrindo um preview somente-leitura no mesmo layout visual do aluno (nome, mídia, descrição, prescrição), sem nada que grave dado do aluno; (3) bug de perf corrigido — `VoltageBar` tentava renderizar 1 segmento por unidade de `total` (o plano Plus usa `limiteAlunos = 1_000_000` como sentinel de "ilimitado", travando a tela do Personal), agora com teto de 100 segmentos + a contagem de alunos some por completo no plano Plus ("Alunos ilimitados"); (4) dashboard do aluno ganha um empty-state único de primeiro acesso ("Começar agora" / "Tem seu próprio Personal?"), substituindo 3 mensagens soltas e sobrepostas que só apareciam quando o aluno não tinha nada ainda
- [x] Fase 66: dashboard do Personal redesenhado a partir de mockup do fundador — 2 cards de ação clara: "📋 Biblioteca de Templates" ("⚡ Explorar Templates" → `/personal/programas`; "ou monte um programa do zero →" → mesma tela já com o formulário aberto via `?criar=1`) e "👥 Meus Alunos" (vincular aluno, contagem/limite, "Gerenciar alunos →", e um acesso rápido embutido pra "Dúvidas de alunos (N pendentes)" — conta client-side as threads `ABERTO`, sem endpoint novo). "Montagem Inteligente" saiu do dashboard e passou a morar em `/personal/programas`, junto do formulário manual de criar template (agora escondido atrás de um botão "+ Criar template" em vez de sempre aberto no topo da tela) — a tela primeiro mostra os templates já criados (Meus/Básico/Premium), a criação vem depois
- [x] Fase 67: ordem das seções de `/personal/programas` revisada — Templates Premium primeiro, Templates Básico em seguida, "Criar um novo template" penúltimo, "Meus Templates" (os do próprio Personal) por último (antes vinha primeiro)
- [x] Fase 68: catálogo de exercícios ganha 2 categorias expandidas — `Cardio` (foco em equipamento de academia: Assault Bike, Ergômetro de Braço, Versaclimber, Ski Erg, Sled Push + condicionamento com kettlebell/halteres) e `Alongamento` (pescoço, trapézio, antebraço/punho, mobilidade torácica/tornozelo, banda iliotibial, grande dorsal, pombo), de 10 pra 21/22 exercícios cada (`prisma/seed-cardio-alongamento.ts`, idempotente) — todo `mediaUrl` é um vídeo do YouTube real e verificado via oEmbed (curto, de canal/visualização confiável), nenhum link inventado; candidatos sem vídeo bom o suficiente foram descartados em vez de entrar com um link fraco. `data/exercises_seed.json` (bootstrap de banco novo) atualizado junto, mantendo os testes de contagem do catálogo consistentes
- [x] Fase 69: 5 novos templates PREMIUM tagueados `EXPRESS` (sessões curtas de alta densidade metabólica) — Hipertrofia Express 3X, Metabolic Burn 40, Esculpimento Express, Força & Volume 40, Pico de Hipertrofia 5X — com bi-set antagonista/drop-set/rest-pause/cluster-set/myo-reps em `notes` + tradução EN/ES
- [x] Fase 70: `WorkoutTag` ganha 3 valores de nível (`INICIANTE`/`INTERMEDIARIO`/`AVANCADO`); 4 novos templates PREMIUM femininos (Cintura Fina & Bumbum VIP, Curvas Definidas Pro, Glúteos de Ouro Extreme, Efeito Sereia Sculpt) + nível aplicado retroativamente aos 22 templates PREMIUM pré-existentes (nível confirmado pelo fundador)
- [x] Fase 71: filtro por tag (chips) adicionado em `/personal/programas` (Premium + Básico), mesmo padrão do carrossel do aluno
- [x] Fase 72: filtro do carrossel Premium do aluno vira 2 grupos de chip independentes (foco + nível, combináveis)
- [x] Fase 73: filtro de `/personal/programas` vira multi-seleção (2+ tags ao mesmo tempo, "OU" entre elas), com tom distinto pras 3 tags de nível
- [x] Fase 74: telas de "Descoberta de Profissionais" (Fase 21) que nunca existiram no lado do Personal — nova seção no perfil do Personal com o toggle `availableForNewStudents` ("Aparecer no diretório"), cidade/UF estruturados (`User.city`/`state`, substituindo o antigo `location` texto livre nunca preenchido de verdade), especialidades (`Specialty[]`, lista fixa de 10, múltipla escolha) e preview ao vivo do card antes de salvar. Lado do aluno: campo de cidade/UF direto na tela de busca (`/profissionais`, salva no usuário após a primeira busca pra pré-preencher da próxima vez) + atalho "usar minha localização atual" (geolocalização do navegador + reverse-geocoding via Nominatim/OpenStreetMap, sem chave) + card de cidade também em `/perfil`. Preço não é exposto no catálogo (decisão do fundador, fase focada em localização/especialidade/bio)
- [x] Fase 76: "Solicitar vínculo" (clique cego) vira "Enviar mensagem" — a 1ª mensagem do aluno cria a mesma `ConnectionRequest` de sempre (nova tabela `ConnectionMessage`, presa a ela); os dois lados trocam mensagens livremente enquanto a conversa não for recusada (aluno em "Minhas conversas" no `/profissionais`, Personal direto em `/personal/solicitacoes` antes de decidir aceitar/recusar). Aceitar continua criando o `ClientRelation` real exatamente como antes — nada do fluxo de aprovação/limite de alunos mudou. Notificação de nova mensagem via o mesmo `notificationsService` já usado por todo o resto do app (in-app, REST puro — já reusável por um futuro cliente mobile sem mudança nenhuma de backend)
- [x] Fase 77: login com SSO Google (`POST /api/auth/google`, botão "Entrar com o Google" via Google Identity Services, sem dependência npm nova no client). Vincula automaticamente por e-mail verificado numa conta tradicional já existente; conta nova passa pela mesma tela de escolha de papel (ALUNO/PERSONAL) já usada no cadastro tradicional, sem exigir senha (`User.passwordHash` agora nullable). `GOOGLE_CLIENT_ID` não é secreto — plugado via Terraform (`var.google_client_id`) tanto no runtime do backend quanto como build-arg do frontend (NEXT_PUBLIC_* precisa existir no build, não no runtime do Cloud Run). Domain mapping do Cloud Run iniciado pro domínio thunderafit.com.br (registro.br) — aguardando verificação de propriedade via Search Console antes do `gcloud beta run domain-mappings create` completar; certificado gerenciado pelo Google (renovação automática, sem Let's Encrypt/cron necessário)
- [x] Fase 78: "Fale Conosco" (menu do Aluno/Personal/Nutricionista) — título + mensagem (≤500 chars) sempre gravados (`ContactMessage`), envio de e-mail best-effort via Gmail SMTP (nodemailer, sem custo, sem serviço terceiro) para `eliel.garcia@gmail.com` por padrão; falha no e-mail nunca derruba a gravação da mensagem. `CONTACT_GMAIL_APP_PASSWORD` fica no Secret Manager (ainda sem versão real — precisa da App Password de verdade antes do próximo `terraform apply`)
- [x] Fase 79: corrigido bug relatado — clicar numa notificação só marcava como lida, não abria nada. Resolvido 100% no client (`resolveNotificationPath`, sem coluna nova): mapeia `(type, papel de quem está vendo)` pra a tela certa (solicitação → `/personal/solicitacoes`, mensagem → tela certa por papel, dúvida → tela certa por papel, etc.)
- [x] Fase 80: botão "Trocar senha" no perfil (aluno e Personal) — `User.passwordHash` nullable permite tanto trocar (exige senha atual) quanto DEFINIR pela 1ª vez (conta só-Google, Fase 77, sem senha atual pra conferir). Tela de admin de usuários ganha foto+nome (fallback pro e-mail quando não há nome) em vez de só e-mail, e um botão "Remover" com confirmação inline (mesmo padrão de `RoleEditor`/`PremiumEditor`) que apaga o usuário definitivamente — cascade manual cobrindo ~10 tabelas sem FK real pra `User` (nenhuma tem), preservando o histórico de treino de OUTROS alunos quando o removido é um Personal (só orfanota o `personalId`, nunca apaga o programa de quem continua existindo); `AdminAccessLog`/`AdminAuditLog` nunca são tocados (trilha de auditoria sobrevive ao ator/alvo removido)
- [x] Fase 81: confirmação de e-mail + "esqueci minha senha" + "excluir minha conta" (self-service). Pesquisa de melhores práticas (OWASP Forgot Password / Email Verification Cheat Sheets) antes de implementar, dado o serviço ser monetizado: tokens de 256 bits (`crypto.randomBytes(32)`, bem acima do mínimo de 128 bits recomendado), guardados só como hash sha256 (não bcrypt — o token já nasce com entropia alta, bcrypt não agrega nada e só custaria CPU à toa), expiração curta (24h verificação de e-mail, 1h reset de senha — dentro da janela de 15–60min recomendada), resposta de "esqueci minha senha" SEMPRE genérica (mesma mensagem exista ou não a conta, anti-enumeração), reset de senha invalida todas as sessões existentes (`refreshTokenHash = null`), e reaproveita o `loginRateLimiter` já existente (por `IP+email`/`IP+uid`) nos 2 novos endpoints públicos. Cadastro tradicional dispara e-mail de verificação automaticamente (best-effort, nunca derruba o cadastro); conta via Google SSO já nasce verificada (Google já validou `email_verified` antes de emitir o idToken). "Excluir minha conta" reaproveita 100% o cascade manual da Fase 80 (`src/lib/user-deletion.ts#deleteUserCascade`, extraído do admin pra um módulo compartilhado) — exige a senha atual quando a conta tem uma, bloqueia se for o último ADMIN restante (mesmo guard do admin), e limpa os cookies de sessão ao final. Frontend ganha 3 telas públicas novas (`/verificar-email`, `/esqueci-senha`, `/redefinir-senha`), um banner "confirme seu e-mail" (com botão de reenvio) visível em toda tela autenticada enquanto `emailVerifiedAt` for null, link "Esqueci minha senha" na tela de login, e um card "Excluir minha conta" (confirmação em 2 etapas, avisos explícitos de perda de dado) nos perfis de aluno e Personal. Bug real pego em teste manual antes do deploy: a 1ª versão só filtrava `passwordHash`/`refreshTokenHash` do retorno da API — os 4 campos novos de token (hasheados, mas ainda assim não deveriam vazar) apareciam em toda resposta JSON de usuário; corrigido com um helper único `toSafeUser()` reaproveitado nos ~8 lugares que devolviam o usuário
- [x] Fase 82: redesign da tela inicial (`/login`, etapa de e-mail) com tratamento de landing — headline + subheadline de marketing acima do formulário, e rodapé de disponibilidade honesto ("só Web por enquanto" — Android/iOS ainda não publicados, só o spike técnico da Fase 19, decisão explícita de não prometer o que ainda não existe). Os 2 botões de seleção de papel no cadastro (antes texto puro, mesmo peso visual) viram cards com ícone (🏋️ Personal / ⚡ Aluno) + título + descrição, acendendo em dourado (Personal) ou ciano (Aluno) quando selecionados — mantém a mesma convenção de cor por papel já usada no resto do produto em vez de introduzir uma cor de seleção única, por consistência. Verificado visualmente via Playwright (screenshot) antes do deploy — precisou baixar browsers do Playwright nesta sessão (versão instalada dessincronizada da versão do pacote).
- [x] Fase 83: troca do Gmail SMTP pessoal (Fase 78) pelo Resend, com domínio próprio verificado (`thunderafit.com.br`, SPF/DKIM/DMARC configurados no registro.br) — remetente profissional (`no-reply@thunderafit.com.br`) em vez do e-mail pessoal do fundador, melhor entregabilidade, ainda grátis no volume atual (tier grátis do Resend, 3.000 e-mails/mês). `src/lib/mailer.ts` reescrito (mesma assinatura `sendMail()`, nada mais no domínio de auth/contato precisou mudar); `CONTACT_GMAIL_USER`/`CONTACT_GMAIL_APP_PASSWORD` removidos (secret antigo destruído via `terraform apply`), `RESEND_API_KEY` novo (único secret, sem "senha de app" separada). Auditoria de segurança pedida pelo fundador antes do deploy revelou uma lacuna real: `resend-verification` (autenticado) e `verify-email` (público) não tinham NENHUM rate limit — só `forgot-password`/`reset-password` (Fase 81) já tinham. Corrigido reaproveitando o mesmo `loginRateLimiter` (5 tentativas/15min) nos 2 endpoints que faltavam, mesmo padrão de "freio de chamadas" já usado em `checkEmailHandler`. Incidente real durante o desenvolvimento: a chave real do Resend foi colocada no `.env` local pra um teste manual pontual — como `setup.ts` (global a todos os testes Jest) carrega o mesmo `.env` e alguns testes (`admin.test.ts`) não mockam o mailer, cada rodada de suíte completa disparou e-mails de verdade via `register()` em loop, estourando a cota diária grátis em poucas rodadas de debug; corrigido removendo a chave do `.env` (sem custo financeiro, só a cota resetando em 24h — nenhum dado vazou). Drive-by: `admin.test.ts` tinha um timeout pré-existente de `beforeAll` (5s padrão do Jest, insuficiente pro loop de registro de vários usuários) — corrigido com o mesmo timeout de 30s já usado em `admin-delete-user.test.ts`, não é uma regressão desta fase.
- [x] Fase 84: link suplementar do YouTube por cima do vídeo/GIF próprio do exercício. Campo novo `Exercise.youtubeSupplementUrl` (nullable, migration aditiva) — só relevante quando `mediaType` é VIDEO/GIF (quando é YOUTUBE, o `mediaUrl` já É o link, o badge não aparece, comportamento 100% preservado pedido pelo fundador). Badge circular pequeno no canto inferior direito do vídeo/GIF (posição escolhida entre 3 opções apresentadas ao fundador), extraído pra um componente novo `ExerciseMedia` compartilhado entre `ExerciseExecutionCard` e `ExercisePreviewCard` (antes duplicavam o mesmo bloco de renderização). Tela de admin (`/nimbus/exercicios`) ganha um campo "Link do YouTube com a execução completa" visível só quando o tipo de mídia é VIDEO/GIF — pedido extra do fundador ("reaproveite a url atual"): quando o admin troca de YOUTUBE pra VIDEO/GIF, o campo já vem PRÉ-PREENCHIDO com o link do YouTube que já existia (frontend), com uma rede de segurança equivalente no backend (se o campo não vier no body e a mídia anterior era YOUTUBE, reaproveita automaticamente o `mediaUrl` antigo) — dá pra editar/reaproveitar/limpar antes de salvar. `updateExerciseMedia` (admin) ganhou suporte a atualizar só o link suplementar sem reenviar arquivo, quando o exercício já é aquele mesmo tipo de mídia própria. Drive-by: 2 outras suítes de teste (`workout-programs.test.ts`, e intermitentemente outras) vinham estourando o timeout padrão de 5s do Jest em `beforeAll`s com múltiplos registros/logins sequenciais sob carga da suíte completa — em vez de corrigir arquivo por arquivo, subiu o `testTimeout` padrão pra 20s direto no `jest` do `package.json`, resolvendo a causa raiz de uma vez (não é uma regressão desta fase, só ficou visível rodando a suíte completa repetidas vezes durante o desenvolvimento).
- [x] Fase 85: Aluno Premium ganha a capacidade de montar/editar o próprio treino do zero (antes só dava pra aplicar templates curados pelo admin — nenhuma criação livre existia). Card novo de upsell (`PremiumUpsellCard`, layout pedido pelo fundador via mockup — 🔒 + eyebrow PREMIUM + headline + 3 benefícios + CTA de borda tracejada "⚡ Testar 7 dias grátis") no dashboard do aluno e em `/meu-treino-pessoal`, auto-contido (busca o próprio status e cuida do teste grátis, some sozinho pra quem já tem acesso). Fluxo de criação simplificado: só pede um nome (`/meu-treino-pessoal/criar`), a 1ª sessão "Treino A" já é criada automaticamente em seguida, caindo direto na tela de adicionar exercícios — sessões extras (B, C...) entram depois por um botão incremental, mesmo padrão do Personal. Backend novo (`createSelfProgram`/`addSelfSession` em `workout-programs.service.ts`, `addSelfExercise`/`moveSelfExercise`/`deleteSelfExercise` em `workouts.service.ts`) espelha as regras já usadas pro Personal (mesma checagem de posse `origin`+dono, mesma trava de "1 treino pessoal ativo por vez", mesmo gate de billing do Aluno Premium — 402 `PREMIUM_REQUIRED`) — **exceto excluir o próprio treino, que nunca é bloqueado por premium vencido** (remover conteúdo não pode ficar preso atrás de um paywall). Os endpoints de adicionar/mover/remover exercício (`POST/PUT/DELETE /api/workouts/:id/exercises...`) passam a ramificar por role no controller — Personal continua chamando exatamente o mesmo código de sempre, comportamento 100% inalterado (confirmado com um teste de regressão dedicado). Frontend reaproveita ao máximo: `AddExerciseForm`/`ExerciseReorderButtons`/`ExerciseDeleteButton` funcionam sem nenhuma mudança (já batiam nos mesmos endpoints, agora role-aware no backend); `/programas/[id]` ganha um lápis ✏️ de edição por sessão + "Adicionar treino"/"Excluir" quando o treino é `origin: SELF` do próprio aluno (nunca no prescrito pelo Personal, que continua sem nenhum controle de edição). Treino "não pode editar nem trocar o que o Personal prescreveu" confirmado intacto — regra só se aplica a `origin: SELF`, nunca a `origin: PERSONAL`. Sem migration nesta fase (nenhum campo novo de schema).
- [x] Fase 86: convite pro Personal (dashboard do aluno) e pro aluno (`VincularAlunoForm`, quando o e-mail buscado não existe ainda) trocam "copiar convite" por um link direto `https://wa.me/?text=...` — mensagem pré-preenchida, abre o WhatsApp (app no celular, Web no desktop) direto no escolher-contato, sem copiar/colar. Helper único (`frontend/lib/whatsapp.ts#buildWhatsAppShareUrl`) reaproveitado nos dois lugares. Nenhuma dependência nova — `wa.me` é o mecanismo padrão que qualquer site usa pra abrir o WhatsApp com texto pronto, sem SDK nem autenticação (diferente do "share sheet" genérico já usado no resumo pós-treino, que deixa o usuário escolher entre vários apps — aqui o pedido era ir direto pro WhatsApp). Investigação à parte (sem alteração de código): erro `origin_mismatch` do Google OAuth ao logar via `thunderafit.com.br` — o domain mapping do Cloud Run já cobre tanto `thunderafit.com.br` quanto `www.thunderafit.com.br` (confirmado via `gcloud beta run domain-mappings list`), mas o Client ID OAuth (criado manualmente no Console na Fase 77, antes do domínio próprio existir) provavelmente só tem a URL original do Cloud Run (`https://thunderafit-frontend-vy6oiie6rq-uc.a.run.app`) na lista de "Authorized JavaScript origins" — precisa adicionar os 2 domínios próprios lá manualmente (não há comando `gcloud` pra isso, é Console-only em APIs & Services → Credentials).
- [x] Fase 87: preços definidos pelo fundador pra ativar a monetização real — Personal Base R$14,99/mês, Personal Plus R$29,90/mês (nomes internos `BASE`/`PLUS` mantidos, sem rename), ambos com **trimestral 20% off no lugar do anual** (removido — o fundador decidiu não oferecer mais compromisso anual). Aluno Premium também ajustado: R$9,99/mês (era R$9,90) após os 7 dias grátis, desconto trimestral unificado em 20% (era 30%, agora igual aos planos do Personal). Rename sistêmico de `"annual"` pra `"quarterly"` em todo o stack (tipo `BillingInterval`, `stripePriceEnvVar`, controller/service de checkout, setup de teste, `frontend/lib/api/billing.ts`, `/personal/upgrade`, i18n pt/en/es, E2E) — nenhuma lógica de negócio nova, só terminologia/preço (o backend de billing Stripe já estava 100% implementado e testado desde antes). `src/billing/BILLING_SETUP.md` atualizado com os novos Price IDs esperados (`STRIPE_PRICE_ID_{BASE,PLUS}_{MONTHLY,QUARTERLY}`). Início do onboarding real da conta Stripe (test mode) guiado passo a passo pelo fundador, que nunca tinha usado a plataforma — ainda faltam: criar os 4 produtos/preços reais no Dashboard, configurar o webhook de produção, e wire dos 6 env vars no Terraform (`infra/secrets.tf`/`variables.tf`/`cloud_run.tf` — hoje nenhuma variável Stripe existe lá, gap real encontrado nesta fase, não corrigido ainda).
- [x] Fase 88: corrigida inconsistência no dashboard do aluno (`/dashboard`) — o Bloco 1 ("Prescrito pelo seu Personal", Fase 36) aparecia mesmo quando o aluno não tinha nenhum Personal vinculado, só pra abrigar o card de convite (`InvitePersonalCard`), prometendo uma seção de prescrição que não existia e empurrando o treino pessoal do aluno (Bloco 2, "Meus Treinos Pessoais") pra debaixo de um rótulo que não era dele. Agora o Bloco 1 só renderiza quando `hasPersonalRelation` é real; sem vínculo, o convite vira card secundário logo depois do treino pessoal, sem a moldura de "prescrito" — mesma decisão de design (esconder o bloco inteiro, treino pessoal sobe pra posição principal) confirmada com o fundador entre 3 abordagens apresentadas. Drive-by: `dashboard-blocos-flow.spec.ts` tinha um teste desatualizado desde a Fase 86 (ainda testava o botão "Copiar convite", hoje é um link `wa.me`) — corrigido, e adicionado um novo caso cobrindo exatamente o cenário desta fase (treino pessoal aplicado, sem Personal vinculado).
- [x] Fase 89: cronômetro da sessão de treino ganha início explícito — botão "▶ Iniciar Treino" no topo da tela (`/treinos/[id]`), substituindo o início implícito da Fase 39 (que já contava o tempo desde a abertura da tela, mesmo que o aluno só ficasse olhando os exercícios antes de treinar de verdade). Contador ao vivo exibido tanto no lugar do botão de início (uma vez iniciado) quanto dentro do próprio botão "Concluir sessão" — o aluno sempre vê quanto tempo já passou, e o resumo pós-treino agora reflete o tempo real de treino, não o tempo desde a abertura da página. Persistido em `localStorage` por treino (`workout-session-timer.ts`), sobrevive a refresh/fechar aba sem precisar de nenhum campo novo no backend — mesmo espírito 100% client-side do cronômetro anterior. Mecanismo de proteção contra "cronômetro infinito" (aluno inicia e não conclui, ou esquece a aba aberta): padrão de mercado de aviso + prazo de graça (mesma ideia do "Ainda está aí?" do Zoom/Google Docs) — 30min sem nenhuma atividade (clique, tecla, série marcada) mostra um modal "Ainda está treinando?"; sem resposta em mais 15min, a sessão se encerra sozinha, contando a duração só até a ÚLTIMA atividade real (não até o momento do auto-encerramento, pra não inflar o tempo com o celular parado no bolso). A mesma regra cobre uma sessão "pendurada" de uma visita anterior (aluno fechou o app e só voltou dias depois) sem precisar de um caminho de código separado. Parâmetros (30min aviso / 15min de graça) decididos com o fundador depois de uma pesquisa rápida de melhores práticas. `formatDuration` extraído de `post-workout-summary-card.tsx` pra `lib/utils.ts`, reaproveitado no novo contador ao vivo. 4 specs E2E atualizadas pra clicar "Iniciar Treino" antes de "Concluir sessão" (botão agora desabilitado até iniciar).
- [x] Fase 90: 2 mecanismos novos na gestão de usuários (`/nimbus/usuarios`). (1) "Marcar e-mail como verificado" por usuário — bypass manual do fluxo real por link (Fase 81), pra casos de suporte (e-mail nunca chegou, conta antiga sem verificação). (2) Concessão manual de plano ganhou um prazo em dias — "brinde por tempo limitado": Personal/Nutricionista podem receber Base OU Plus (antes só dava Plus), Aluno Premium reaproveita o campo de expiração que já existia (Fase 56) — ambos revertem sozinhos (sem cron) na próxima leitura funcional depois de vencer. Campo novo `User.planoAssinaturaExpiresAt` (migration aditiva) só é usado por concessões manuais — o webhook do Stripe sempre limpa esse campo em toda escrita real, nunca reduz uma assinatura paga de verdade por engano. Reversão automática plugada em 3 pontos: checagem de limite ao criar vínculo (`relations.service`), status exibido em `/personal/upgrade` (`billing.service.getStatus`), e a própria listagem de usuários do admin (sempre mostra o estado real, nunca um plano "fantasma"). Gap de segurança real encontrado e corrigido nesta fase: o domínio admin nunca tinha adotado o `toSafeUser()` (Fase 81) — `updateUserRole`/`setUserPremium`/a nova `verifyUserEmail` devolviam a linha inteira do Prisma pro cliente, incluindo `passwordHash`/hashes de token; `toSafeUser` foi extraído de `src/auth/services/auth.service.ts` pra `src/lib/safe-user.ts` (reaproveitado por ambos os domínios agora) e aplicado nos 3 endpoints. `listUsers` em si nunca teve esse problema (já usava `select` restrito desde a Fase 58).
- [x] Fase 91: Termos de Uso e Política de Privacidade — pesquisa das regras vigentes (CDC/LGPD) antes de escrever: uma isenção "não nos responsabilizamos por nada" seria juridicamente nula (CDC art. 51); o texto final usa o mesmo modelo de plataformas de intermediação (Uber/GetNinjas) — o ThunderaFit nunca é o prestador do serviço, quem responde tecnicamente é o Personal/Nutricionista (registro profissional próprio) ou o Aluno Solo quando treina por conta própria. Dados de anamnese são tratados como dado sensível pela LGPD (art. 5º/11) — a política já documenta isso, e o formulário de anamnese (`/anamnese`) ganhou uma checkbox de consentimento explícito e separado do aceite geral dos Termos, obrigatória só na PRIMEIRA vez que o aluno salva (edições seguintes não reconfirmam). Novas páginas públicas `/termos-de-uso` e `/politica-de-privacidade` (só português — documento específico da legislação brasileira, não localizado pro público EN/ES do resto do app); tela de cadastro (`/login`, ambos os caminhos — Google direto e o passo nome/senha) ganha a frase de aceite com os 2 links (`t.rich`, mesmo padrão já usado em `nimbus/alunos/.../anamnese`). Sede declarada em Florianópolis/SC; contato de privacidade reaproveita o e-mail pessoal do fundador já usado no "Fale Conosco" (Fase 78). Fonte canônica do texto em `docs/termos-de-uso.md`/`docs/politica-de-privacidade.md`. **Recomendação registrada nos próprios documentos**: revisão por advogado antes de tratar como definitivo, principalmente as cláusulas de isenção médica e limitação de responsabilidade.
- [x] Fase 92: correção de responsividade em `/nimbus/usuarios` — a linha de cada usuário e os botões de ação (Editar role/Conceder Premium/Marcar e-mail verificado/Remover, incluindo os painéis de confirmação com select/input da Fase 90) não se adaptavam a telas estreitas, causando sobreposição e estouro horizontal. Layout agora empilha em coluna no mobile (`flex-col sm:flex-row`), botões/painéis reduzidos (`h-7`/`h-8`, texto menor) e com `w-full sm:w-auto`, paginação também responsiva. Só CSS/layout, tela de uso interno do admin — sem mudança de comportamento.
- [x] Fase 93: Personal e Aluno ganham onde ver a própria assinatura e quando ela termina. Pesquisa rápida de melhores práticas confirmou o padrão de mercado (status de assinatura integrado à área de conta/configurações, não numa tela solta) — reaproveitado em vez de inventar navegação nova: Personal já tinha o link "Planos" no menu apontando pra `/personal/upgrade` (só faltava mostrar a data de expiração quando existir, agora exibida); Aluno nunca teve NENHUM lugar pra ver isso — resolvido com um card novo em `/configuracoes` (tela já acessível por qualquer role, "nascida pra crescer" desde a Fase 46), mostrando status do Aluno Premium (ativo/teste grátis/cancelado-mas-ainda-ativo/sem premium) + data de expiração quando houver. Campo `planoAssinaturaExpiresAt` (Fase 90, só backend/admin até aqui) agora também exposto em `GET /api/billing/status` pro próprio usuário ver — nunca preenchido numa assinatura Stripe real (só concessão manual do admin), então não conflita com o "Gerenciar assinatura" existente. Gap real encontrado e NÃO corrigido nesta fase (fora do pedido, que foi só "Personal e Aluno"): NUTRICIONISTA usa o mesmo sistema de plano que PERSONAL no backend, mas `/personal/upgrade` só aceita `allowedRoles={["PERSONAL"]}` — um Nutricionista pago não tem hoje nenhum lugar (nem o card novo desta fase, que também restringe por essas 2 roles) pra ver ou gerenciar o próprio plano.
- [x] Fase 94: tela "Fale Conosco" ganha novo texto de abertura ("Sua mensagem é importante, nos ajude a melhorar." + "Dúvidas, sugestões, problemas — o que for, nosso time está pronto pra atender.") e um card de atalho pro Instagram (@thunderafit, abre em nova aba). `lucide-react` (já usado no resto do app, ex: `notification-bell.tsx`) não tem ícones de marca nesta versão — resolvido com um SVG inline do glifo padrão do Instagram, sem dependência nova.
- [x] Fase 95: corrigido bug real do app Android (Capacitor) — o botão/gesto de voltar sempre fechava o app, mesmo navegando dentro dele. Confirmado: é o comportamento nativo padrão do Capacitor quando nenhum listener JS de `backButton` está registrado (documentado no plugin `@capacitor/app`) — não é bug de configuração deste projeto, é algo que todo app Capacitor precisa assumir explicitamente. Resolvido do jeito mais elegante (sem precisar de nenhum botão "Voltar" dentro da UI, como alguns apps têm): `@capacitor/app` instalado + um listener registrado uma única vez (`AndroidBackButtonHandler`, montado em `Providers`, só ativo dentro do app nativo via `Capacitor.isNativePlatform()`) que navega o histórico normal do app (`window.history.back()`, o mesmo já usado pelo router do Next.js) e só MINIMIZA (não mata o processo, convenção do Android) quando não há mais pra onde voltar — evita o re-login/flash de carregamento que um `exitApp()` mais abrupto causaria ao reabrir. `npx cap sync android` registrou o plugin no projeto nativo (`capacitor.build.gradle`/`capacitor.settings.gradle`). Build de debug gerado via WSL (mesmo pipeline manual da Fase Play Store) e reenviado pro bucket de teste já usado pelo fundador, pra reteste imediato.
- [x] Fase 96: os 3 itens de alto impacto da triagem de performance de 2026-07-29 (ver MASTER_SPEC.md, Grupo Y) resolvidos. (1) `admin.service.getOverview` — o `groupBy` na `ClientRelation` inteira + fetch de todos os profissionais pra contar quem bateu o limite Freemium em memória virou uma única agregação SQL (LEFT JOIN + `COUNT(*) FILTER`), sem nenhuma linha de usuário/vínculo atravessando a rede. (2) Waterfall do dashboard do aluno eliminado com um endpoint agregador novo, `GET /api/dashboard/aluno-summary` (domínio novo `src/dashboard/`, sem repository próprio — só compõe as mesmas chamadas de serviço que os 3 endpoints antigos já faziam, preservando 100% da autorização/posse de cada um); o frontend trocou 3 hooks em cascata (lista→detalhe do Personal, lista→detalhe do self, lista→detalhe do plano de dieta) por 1 `useQuery` só. (3) `GET /api/exercises` ganhou o primeiro response schema do Fastify do backend (serialização compilada via `fast-json-stringify`) — escopado só a esse endpoint (payload mais pesado E mais simples/estável) porque um response schema funciona como allowlist de serialização; `getProgram`/`getWorkout` (o outro alvo de alto payload) ficaram de fora de propósito, documentados como próxima fase por exigirem mapeamento campo-a-campo cuidadoso pra não silenciar dado de verdade. O restante da triagem (10 itens de médio/baixo impacto) documentado no Grupo Y do MASTER_SPEC.md pra uma fase futura, junto com 2 fases novas de planejamento: revisão de caso de uso de downgrade do Personal (Grupo Z) e melhoria no cadastro de alunos (Grupo AA, escopo em aberto). *Modelo: Sonnet 5. 3 testes novos (`src/dashboard/__tests__/dashboard.test.ts`), suíte completa sem regressão, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 97: 2ª etapa da triagem de performance de 2026-07-29 (ver MASTER_SPEC.md, Grupo Y) — os 3 itens de baixo risco/rápidos, agrupados numa leva só (config/query pura ou fetch duplicado simples, sem contrato de API nem risco de allowlist de serialização). (1) `completeWorkout` passou a invalidar só `["workout-program", programId]` em vez do prefixo inteiro. **Achado ao implementar**: isso revelou uma regressão real introduzida silenciosamente pela Fase 96 — o resumo agregado do dashboard (`["aluno-dashboard-summary"]`) embute o mesmo programa sob uma chave de cache própria, nunca tocada pela invalidação antiga; corrigido junto (também no fluxo do aluno editando o próprio treino). (2) `staleTime` por família de queryKey via `queryClient.setQueryDefaults()` — um lugar só em vez de repetir em ~15 call sites (`Infinity` pra billing/perfil, minutos pro resto). (3) `workout-programs.service.ts#addSession` parou de carregar o mesmo programa 2x no mesmo request. Os itens maiores (response schema de getProgram/getWorkout, paginação, dynamic import de libs pesadas, escopo do bundle i18n, placeholderData lista→detalhe) ficaram documentados no MASTER_SPEC.md como próximas etapas — cada um precisa de decisão de escopo ou cuidado extra que não cabia numa leva "rápida". *Modelo: Sonnet 5. 463/463 backend, 55/55 Jest/RTL, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 98: 3ª etapa da triagem de performance de 2026-07-29 (ver MASTER_SPEC.md, Grupo Y, item 106) — `placeholderData` na navegação lista→detalhe de programas de treino, tanto do aluno (`/programas` → `/programas/[id]`) quanto do Personal (`/personal/programas` → `/personal/programas/[id]`): o `useQuery` do detalhe semeia o cache a partir do item já carregado na lista (por queryKey + match por id), eliminando o flash de loading na navegação comum, sem chamada de rede a mais. **Dupla checagem campo-a-campo antes de implementar** (pedido explícito do fundador ao mexer nesse código): confirmado que nenhuma das duas telas de detalhe precisa de guard novo — os campos que só existem no detalhe (`suggestedNext` do lado do aluno) já tinham guard falsy pré-existente, e os campos que a lista não traz (contagem de exercícios) já caíam num fallback (`?? 0`). **Achado ao comparar as duas queries de lista no repository**: `listByPersonal` não seleciona `lastCompletedAt` por sessão, diferente de `listByAluno` — confirmado inofensivo (a tela do Personal nunca lê esse campo), não uma inconsistência a corrigir, só registrado pra não precisar redescobrir numa fase futura. *Modelo: Sonnet 5. Sem mudança de backend nesta etapa (159/159 Jest do domínio `fitness` confirmando isso); 55/55 Jest frontend, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 99: 4ª etapa da triagem de performance de 2026-07-29 (ver MASTER_SPEC.md, Grupo Y, item 103) — `html-to-image` e `recharts` deixaram de ser importados eager nas 3 telas que só precisam deles condicionalmente: `PostWorkoutSummaryModal` (`/treinos/[id]`, só monta após concluir a sessão) e `LoadHistoryChart`/`FrequencyChart` (`/evolucao` do aluno e `/personal/alunos/[alunoId]`, só montam quando o histórico/frequência já chegou) viraram `next/dynamic(..., {ssr:false})`. `ssr:false` é seguro porque nenhum dos 3 já renderizava no servidor de verdade (dados vêm de `useQuery` client-side). **Verificação além do padrão**: rodei um `next build` de verdade e, via `curl` num `next start` local, comparei os chunks JS do HTML inicial de `/evolucao` e `/treinos/[id]` contra o conteúdo desses chunks — confirmado que nenhum chunk carregado de cara contém `ResponsiveContainer` (recharts) nem `toPng` (html-to-image), só entram sob demanda. Dupla checagem confirmou os 3 componentes têm exportação nomeada única, sem ref encaminhada pelo wrapper em si, e sem nenhum outro import estático deles no projeto (grep completo) — nenhum risco de prop incompatível na troca. *Modelo: Sonnet 5. `tsc --noEmit` limpo, 55/55 Jest frontend, `next build` sem erro + verificação empírica de bundle. Nenhuma mudança de backend.*
- [x] Fase 100: 5ª etapa da triagem de performance de 2026-07-29 (ver MASTER_SPEC.md, Grupo Y, item 99) — `GET /api/workout-programs/:id` e `GET /api/workouts/:id` ganharam response schema do Fastify (serialização compilada via `fast-json-stringify`), o mesmo ganho já aplicado ao catálogo de exercícios (Fase 96), agora nos 2 endpoints mais aninhados que tinham ficado de fora de propósito. Mapeamento campo-a-campo feito antes de escrever o schema (controller→service→repository→Prisma + toda tradução/campo computado como `suggestedNext`), com fragmentos compartilhados entre os 2 endpoints (`workout-response-schemas.ts`) — não reaproveita o schema do catálogo porque aqui o exercício vem de um `include` sem `select`, com 3 campos a mais. Rede de segurança nova (não só os testes de comportamento já existentes): 2 testes que comparam as chaves de cada nível aninhado da resposta HTTP real contra a lista exata esperada, pra pegar em CI qualquer campo esquecido (ou um campo novo esquecido no schema) numa fase futura. **Achado ao escrever o teste**: o `afterAll` de `workouts.test.ts` apagava `WorkoutExercise` sem apagar `SetLog` antes (nunca dava erro porque nenhum teste anterior tinha criado uma série real ali) — corrigido, mesma ordem de dependência que `workout-programs.test.ts` já usava. Confirmado cruzando contra `frontend/lib/types.ts` que nenhum campo opcional do frontend pertence a estes 2 endpoints especificamente. *Modelo: Sonnet 5. 465/465 backend com `--runInBand` (`npx jest` puro, sem `--runInBand`, produz falhas espúrias por paralelismo contra o mesmo Postgres de teste — usar sempre `npm test`), `tsc --noEmit` limpo. Nenhuma mudança de contrato JSON.*
- [x] Fase 101: 6ª etapa da triagem de performance de 2026-07-29 (ver MASTER_SPEC.md, Grupo Y, item 102) — resolvido o `[CONTRATO-API]` que faltava decidir: investigação prévia mostrou que 3 dos 4 consumidores das listas sem paginação (`dashboard.service.ts`, `/personal/alunos`, `/personal/programas`) dependiam do resultado COMPLETO pra funcionar (`.find()`, `Set`, contagem) — paginação de verdade exigiria reescrever os 3, esforço desproporcional a um risco hoje só teórico (ceilings reais já pequenos: `limiteAlunos` 3/20/1M por plano, 5-7 sessões por programa). Escolhido com o fundador: cap defensivo aditivo — `page`/`pageSize` opcionais (mesmo padrão de `GET /api/admin/users`, único precedente de paginação no backend) com default generoso (`DEFAULT_PAGE_SIZE = 300`, teto 500) que não muda comportamento de nenhum consumidor real hoje, só protege contra crescimento fora do previsto; sem envelope novo na resposta, zero mudança de contrato pra quem não usar os parâmetros. **Bônus do mesmo pedido**: teto fixo de 50 templates por Personal (`MAX_PERSONAL_TEMPLATES`), checado nos 2 caminhos que criam um template (`createTemplate` e `saveInstanceAsTemplate`), resolvendo o gargalo na origem; contagem exposta no frontend reaproveitando o heading já existente ("Meus Templates (12/50)"), sem UI nova. **Achado à parte durante a verificação** (fora do escopo pedido, corrigido na mesma fase): a suíte completa falhava intermitentemente em `setlogs.test.ts` (detecção de PR) — causa raiz era um bug real de corrida entre relógios (Node vs. Postgres) em `detectPersonalRecord`, que comparava `new Date()` (app) contra `loggedAt` gerado por `@default(now())` (banco) pra excluir a própria série recém-criada — uma exclusão desnecessária, já que a query sempre roda ANTES do INSERT no mesmo fluxo. Removido o corte por tempo (agora escopa só por aluno+exercício); `buildPersonalRecords` (versão em lote, ao concluir sessão) não foi tocada, usa `windowStart` com propósito diferente e legítimo. 3 rodadas limpas da suíte completa depois da correção. *Modelo: Sonnet 5. 9 testes novos (fronteira exata do teto — 49→cria, 50→rejeita nos 2 caminhos —, e paginação nos 2 endpoints), 474/474 backend, 55/55 Jest/RTL, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 102: motivado por um problema real de custo reportado pelo fundador (Neon: 11.96 compute-hours em 14 dias, hoje só com ele testando) — não veio da triagem de performance, veio de uma preocupação de negócio concreta sobre escalar pra mais usuários. Investigação confirmou que as 2 técnicas pedidas pra checar já estavam aplicadas (cache em memória + ETag no catálogo de exercícios desde a Fase 49; JWT sem nenhuma consulta ao banco na autenticação) — o poll de 30s do sino de notificação era o ÚNICO ponto do app inteiro que batia no banco em intervalo fixo, independente de uso real, mantendo o autosuspend do Neon sempre resetado enquanto qualquer aba ficasse aberta. Descartado como causa: Cloud Run confirmado scale-to-zero nos 2 serviços, `DATABASE_URL` já no endpoint pooled do Neon, `/health` sem tocar no banco, nenhum cron no repositório. Resolvido com o fundador: poll passivo 30s → 6h (nenhum tipo de notificação hoje é sensível a tempo real; `payment_reminder` em particular é criado no LOGIN do aluno, não por um job de fundo) + abrir o sino (ação que já existe) força uma contagem fresca na hora, sem elemento novo na UI. Ver MASTER_SPEC.md, Grupo Y, item 109 (também resolve a menção antiga do item 43). *Modelo: Sonnet 5. `tsc --noEmit` limpo, 55/55 Jest/RTL. Só frontend — nenhuma mudança de backend.*
- [x] Fase 103: fecha uma lacuna de negócio real, validada com o fundador antes de implementar (múltiplas rodadas de pontos a fechar — carência, escopo do bloqueio, autoatendimento, UX do aluno, tratamento de dado ao desvincular, escopo por papel). Hoje, um Personal que cai de plano (downgrade, cancelamento no Stripe, ou concessão manual do admin vencida) mantinha acesso TOTAL e ILIMITADO aos alunos já vinculados (decisão documentada da Fase 20) — só ficava bloqueado de vincular um aluno NOVO. Fechado: depois de uma carência de 5 dias acima do limite, TODA prescrição/edição de treino pros alunos já vinculados passa a ser recusada (403 `PERSONAL_OVER_LIMIT`), e os alunos afetados perdem acesso de leitura E escrita ao treino daquele Personal especificamente (403 `PERSONAL_PLAN_RESTRICTED`, escopado por PROGRAMA — um aluno com 2 Personals nunca perde acesso ao do outro, que continua em dia). **Achado crítico durante a validação**: não existia NENHUMA forma de um Personal desvincular um aluno (nem endpoint, nem UI) — criado do zero (`DELETE /api/relations/:alunoId`) especificamente como a via de autorregularização, sempre disponível mesmo bloqueado (senão o único jeito de sair do bloqueio seria abrir chamado de suporte); preserva 100% do histórico do aluno, só remove o vínculo. Leitura (listar alunos, ver o programa) nunca é bloqueada pro Personal — precisa disso pra decidir quem desvincular. `invoice.payment_failed` (achado da mesma investigação: caía num `default` sem ação) agora dispara um aviso proativo pro Personal, sem mudar o plano ali (isso continua sendo `customer.subscription.updated`, que o Stripe já dispara na primeira falha real). Banner no `AppHeader` (mesmo padrão de `EmailVerificationBanner`) avisa o Personal durante a carência e quando já bloqueado; mensagem do aluno bloqueado usa estilo neutro (não vermelho/alarmante) tanto no `QueryError` compartilhado quanto na tela de execução de treino — pesquisado deliberadamente (Slack esconde sem apagar quando o free tier estoura; SaaS billing UX pede explicação clara sem parecer bug). **Achado de inconsistência ao tocar o código**: `addSessionMutation` em `/personal/programas/[id]` mostrava um erro genérico fixo em vez do texto real do backend (diferente de `applyMutation`, bem ao lado) — corrigido, senão a mensagem nova do bloqueio nunca apareceria ali. *Modelo: Sonnet 5. 1 migration aditiva (`User.overLimiteAlunosSince`, nullable, sem backfill necessário). 24 testes novos (gate isolado, endpoint de desvincular, webhook, ponta a ponta via rotas HTTP reais), 498/498 backend, 55/55 Jest/RTL, `tsc --noEmit` limpo nos dois lados, `prisma migrate diff` confirmando zero schema drift.*
- [x] Fase 104: motivado pelo fundador diretamente — "hoje pra o personal fazer a vinculação do aluno é somente colocando o email e esperando ele aceitar o vínculo, não acho isso bom em questão de usabilidade". Pesquisado (padrões de convite por link em SaaS + apps de personal trainer) e validado ponto a ponto com o fundador antes de implementar. Substitui por completo o vínculo direto por e-mail: agora o Personal gera um convite só com um apelido (não precisa saber o e-mail do aluno de antemão — maior atrito do fluxo antigo), recebe um link único (token de 256 bits, 7 dias de validade, mesmo padrão de token já usado em verificação de e-mail/redefinição de senha) pra compartilhar no WhatsApp; quem abrir aquele link específico e completar o cadastro (ou logar, se já tiver conta) vira o aluno vinculado automaticamente — sem aceite formal nem precisar voltar depois. Consumo é melhor-esforço nos 3 pontos de entrada (cadastro tradicional, login, SSO Google) — nunca derruba o cadastro/login de quem está se vinculando, com proteção atômica contra 2 chamadas concorrentes tentando consumir o mesmo token. `/login` pula a escolha de papel quando vem de um convite válido (já sabe que é ALUNO) e mostra de quem é o convite. `/personal/alunos` ganhou uma seção de convites pendentes com selo Pendente/Expirado e revogar. **Achado de código morto causado pela própria mudança**: `lookupAlunoByEmail`/`createRelation` (funções do frontend) ficaram sem consumidor nenhum depois da reescrita do formulário — removidas (junto do teste dedicado); os endpoints do backend foram mantidos intactos de propósito (`POST /api/relations` segue ativo internamente, chamado pelo consumo do convite e pelo domínio de conexões). 2 specs Playwright atualizadas pro fluxo novo (não executadas nesta sessão). **Achado registrado, não implementado agora**: links compartilhados sempre abrem no navegador do celular, nunca dentro do app — falta configurar Android App Links, registrado junto do checklist de publicação na Play Store (Grupo X do MASTER_SPEC) já que não bloqueia nem muda o formato da URL do convite. *Modelo: Sonnet 5. 1 migration aditiva (`ClientInvite`, tabela nova). 16 testes novos de backend, teste de componente reescrito, 514/514 backend, 52/52 Jest/RTL, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 105: correção pós-lançamento — 2 bugs reais reportados pelo fundador depois de testar a Fase 104 com um usuário real ajudando a validar. **(1) Banner de contexto do convite quebrava o layout em mobile**: usava `rounded-full` (pill, adequado só pra texto curto) com uma mensagem de 2 frases e pouco padding — corrigido pra `rounded-lg` + `w-full` + mais padding vertical. **(2) Bug crítico — aluno ficava "órfão"**: quem abria o link do convite já autenticado (sessão de uma visita anterior) via só o formulário de e-mail de novo; como o consumo do convite só rodava dentro do SUCCESS do register/login/SSO Google, alguém que nunca completasse aquele formulário especificamente (por já ter sessão) nunca disparava o consumo — cadastro existia, vínculo nunca acontecia, sem nenhum aviso do motivo. Causa raiz: `/login` nunca lia sessão existente (`useAuthStore`), só escrevia. Corrigido com endpoint dedicado autenticado `POST /api/client-invites/consume` — consome o convite de quem já está logado, sem pedir senha de novo; `clientInvitesService.consumeInvite` passou a devolver `{consumed, reason}` em vez de engolir tudo silenciosamente (os 3 callers best-effort continuam ignorando o retorno de propósito). `/login` ganhou 2 telas dedicadas quando `?invite=` é válido e já existe sessão: ALUNO vê "Vincular agora" (1 clique); outro papel vê explicação + "Sair e continuar" (desloga e cai no fluxo normal). **Transparência**: `gcloud` CLI não estava disponível neste ambiente — a causa raiz foi confirmada por leitura de código (fluxo completo de `/login` + os 3 callers de `consumeInvite`), não por inspeção direta dos logs de produção oferecidos pelo fundador; o registro específico do usuário-teste que ficou órfão continua sem reconciliar em produção — o fundador prefere cuidar disso manualmente depois do deploy. *Modelo: Sonnet 5. Sem migration. 6 testes novos de backend (endpoint dedicado: sucesso ALUNO, 403 papel errado, 400 sem token/inválido/expirado/já consumido), 520/520 backend, 52/52 Jest/RTL, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 106: mais 2 bugs reais reportados pelo fundador via print de tela, depois de continuar validando com o mesmo usuário-teste. **(1) O sino de notificações quebrava em mobile de verdade** (achado corrigido agora: a Fase 105 tinha corrigido o banner ERRADO — o de contexto do convite em `/login` — mas o bug relatado originalmente já era este). Causa raiz: o dropdown usava `absolute right-0` relativo só ao próprio botão do sino, que não fica na borda direita do header (avatar/hambúrguer/Sair vêm depois dele); num celular estreito isso jogava a lista (320px) pra fora da tela pela esquerda, cortando o texto — exatamente o que o print mostrava. Corrigido com posicionamento `fixed` (ancorado no viewport) abaixo de `sm`, voltando a `absolute` ancorado no próprio botão a partir de `sm` (onde já funcionava). **Dupla checagem no código aberto pra esse fix encontrou o mesmo bug, um pouco menos grave, no menu de trocar foto de perfil** (`AvatarUpload`, mesmo `app-header.tsx`) — mesmo problema, mesma correção aplicada preventivamente ali também, antes de virar reclamação. **(2) Lembrete de pagamento do Personal pro aluno não chegava**: `checkAndFireDueReminders` só era chamado em `login()`/SSO, mas o access token dura 15min e o cliente renova a sessão via `POST /api/auth/refresh` (cookie httpOnly) de forma transparente sempre que leva 401 — um aluno que já estava "sempre logado" (o padrão de uso mais comum, sessão viva dentro da janela de 7 dias do refresh token) nunca chamava `/api/auth/login` de novo depois do primeiro login, então o lembrete nunca disparava pra ele. Corrigido chamando a mesma checagem também dentro de `refresh()` — mantém a filosofia "sem scheduler, checa em toda interação real" já documentada nessa função, só fecha o caminho que faltava. *Modelo: Sonnet 5. Sem migration. 1 teste novo de backend (lembrete dispara via refresh, sem novo login), 522/522 backend, 52/52 Jest/RTL, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 107: correção sequencial dos achados priorizados da `docs/AUDITORIA-2026-07-31.md` (auditoria ampla de 2026-07-29→31, ver Fase anterior a esta), em 5 lotes/commits na branch `fix/fase107-auditoria-billing-authz-frontend`. **Billing** (B1/B2/B3/B5/B9/B10/B12): downgrade não-terminal (`past_due`/`unpaid`) passa a manter `stripeSubscriptionId` (`applyInactivePlan`, novo) em vez de zerá-lo como o cancelamento de verdade (`applyFreePlan`) — sem isso, a recuperação de pagamento (`updated(active)` da MESMA subscription) era rejeitada pelo guard de reordenação pra sempre (B1); checkout e reversão de concessão expirada (`revertExpiredPersonalPlan`) passam a confirmar o estado AO VIVO no Stripe (`subscriptions.retrieve`) em vez de confiar em metadata congelada ou assumir FREE (B2/B3); segundo Checkout bloqueado se já existe assinatura ativa (B10); upgrade pago limpa a carência de excesso de alunos antiga (B5); erro permanente (Prisma `P2025`/`P2002`) no webhook passa a ser reconhecido (200) em vez de gerar retries inúteis por 3 dias que arriscam o Stripe desabilitar o endpoint pra todo mundo (B9); aviso de falha de pagamento só dispara se for da subscription corrente (B12). **Authz pós-desvínculo** (X7/X8, decisão confirmada com o fundador): ao desvincular um aluno, o Personal perde acesso de leitura ao treino/histórico dele (`getWorkout`/`getProgram` revalidam `ClientRelation`), e o aluno deixa de ficar sujeito ao estado de plano do ex-Personal (`assertAlunoWorkoutAccessible` só bloqueia se o vínculo ainda existir) — o histórico do PRÓPRIO aluno nunca é tocado, só o acesso de terceiro. **Auth** (A1/A2/A3/A6/A7/A8/A9/A10/A11): e-mail normalizado (trim+lowercase) em toda leitura/escrita (`register`/login/SSO Google/`findByEmail`) — precedida de checagem read-only em produção por duplicata de case antes de aplicar (ver ressalva abaixo); `loginOrRegisterWithGoogle` passa a tentar `findByGoogleId` antes de `findByEmail` (conta cujo e-mail mudou no Google volta a casar); lembrete de pagamento e `refresh()` blindados com try/catch pra uma falha ali não derrubar o rotacionamento do token; `register` valida tamanho de senha e formato de e-mail (antes só client-side) e converte corrida de unique-constraint em 409; rate limiter de login ganha TTL/sweep (parava de crescer sem limite); `hydrate()` do store tolera JSON corrompido no localStorage; botão do Google tenta carregar de novo após falha em vez de travar pra sempre. **Fitness** (F1/F3/F4/F9/F10/F11, X1): `applySelfTemplate` valida a origem do template ANTES de apagar o treino atual do aluno (evitava perda de histórico de séries em erro de id); prescrição e registro de série ganham validação numérica (sets/reps/carga negativos ou zero não são mais aceitos); erros do domínio passam a incluir `code` na resposta (destrava o tratamento de UI já existente pra `PERSONAL_PLAN_RESTRICTED`); preview de convite não conta mais como tentativa "falha" no rate limiter; paginação default/máxima aumentada (300/500 → 1000/2000, ceilings reais já superam o cap antigo); comparação de volume corrigida pra treino de peso corporal; `NUTRICIONISTA` deixa de conseguir criar/editar/aplicar programa de treino (só endpoint de sessão avulsa tinha sido fechado na Fase 17 — o de programas ficou de fora até agora), UI de programas do Personal também deixa de aceitar esse papel. **Connections/Admin/Support** (C1/C3/C5/C9/C10/C11, X4): aceitar solicitação tolera o vínculo já existir por fora em vez de deixar a linha presa em PENDENTE pra sempre; desvincular aluno agora também limpa a `ConnectionRequest ACEITA` (senão o aluno nunca mais conseguia solicitar aquele profissional de novo, 409 permanente) — achado um bug de FK real ao testar (`ConnectionMessage` sem cascade), corrigido junto; mudança de role + log de auditoria viram uma transação atômica; paginação e corpo de "adicionar exercício ao template" validam antes de chegar no Prisma (eram 500 opacos); mensagem de erro de categoria corrigida; leitura de thread de suporte pelo admin passa a gerar `AdminAccessLog` (dado de saúde, mesma exigência que já valia só pra anamnese). **Frontend** (Fr1/Fr2/Fr3/Fr4/Fr5/Fr6/Fr7/Fr9-Fr12/Fr15-Fr20, parte de Fr13/Fr14/C8): concluir treino não dispara mais 2x nem perde a duração em falha de rede (`clearWorkoutSession` só roda em `onSuccess`); busca em `/profissionais` não trava mais pra sempre quando o perfil falha; adicionar sessão em 4 telas passa a invalidar a query certa (inclusive a contagem de sessões na lista de templates do Personal, Fr19); aceitar solicitação invalida `billing-status` (banner de carência aparecia só depois de um erro 403); textos cortando em celular estreito corrigidos (`flex-wrap`/`break-all`/`break-words` em 6 telas/componentes); mensagens reais do backend (402 Premium, 403 etc.) deixam de ser engolidas por texto fixo em 3 telas; campo "salvar como template" para de reinjetar o nome antigo a cada backspace apagado; carrossel de templates ganha foco por teclado; altura/peso da anamnese com vírgula (`NaN`→`null` salvo silenciosamente) ganham validação e feedback visível. **Deliberadamente NÃO corrigido nesta fase** (registrado, não esquecido): **C4** (corrida de limite de alunos em aceite duplo-clique simultâneo — precisa de constraint de banco ou transação serializável com retry, risco/esforço desproporcional ao tamanho real da janela); **C6/C7** e o restante de **Fr13** (queries silenciosas em `notification-bell`, `/nimbus/treinos-pessoais`, `/dashboard`, `/duvidas`, parte de `/personal/alunos` — mesmo padrão do que foi corrigido, só não coube nesta leva); **B4/B6/B8/B11/B13** (baixo risco/raro, ou exigem decisão de produto antes de codar); **F6/F7/X1(fluxos restantes)/X2/X3/X5/X6/X9-X21** e em particular os 4 gaps que só existem porque **NUTRICIONISTA é um papel descontinuado, sem UI e sem previsão de uso futuro** (avaliado explicitamente com o fundador: não compensa construir tela nova pra um papel que não vai ser usado — registrado aqui como decisão consciente, não lacuna esquecida). **Ressalva em aberto**: a checagem read-only em produção por e-mails duplicados por case (pré-requisito acordado antes de normalizar e-mail, A2) não foi concluída nesta sessão — o classificador de segurança do Claude Code bloqueou a re-tentativa de acesso ao banco de produção; a normalização de código foi implementada mesmo assim (é retrocompatível e correta independente do resultado da checagem), mas a VARREDURA por duplicata existente continua pendente — precisa ser rodada manualmente ou com permissão explícita antes de considerar A2 100% fechado. *Modelo: Sonnet 5. Sem migration. ~35 testes novos entre os 4 lotes de backend, 559/559 backend, 52/52 Jest/RTL, `tsc --noEmit` limpo nos dois lados em cada lote. Mesclada em `dev`/`main` e enviada (`git push origin dev main`) — ver `docs/AUDITORIA-2026-07-31.md` pro detalhe achado-a-achado e o apêndice de resolução.*
- [x] Fase 108: segunda rodada da mesma auditoria, pedida em seguida à Fase 107, fechando os itens deixados como "parcial" ou "não corrigido" que não exigiam schema novo, decisão de produto, nem tocar no papel Nutricionista. **C6** (`/nimbus/treinos-pessoais`: falha ao carregar sessões de um template, adicionar sessão ou excluir não mostrava nada — painel de edição abria vazio com os botões "+ A/B/C" ainda ativos, risco de sessão duplicada por confusão do admin). **C7** (sino de notificações: lista e ações de marcar como lida/todas sem nenhum feedback de erro — dropdown ficava em branco, indistinguível de "sem notificações"). **Resto do Fr13** (dashboard do aluno: vínculo com Personal e resumo semanal; `/personal/alunos`: treinos aplicados e convites pendentes — este último o mais sério do lote, já que a listagem de convites falhando escondia a seção inteira e podia levar o Personal a criar um SEGUNDO convite achando que não tinha nenhum em aberto, com dois links de vínculo automático válidos ao mesmo tempo e nenhum jeito de revogar o primeiro; `/personal/dashboard`: contagem de dúvidas pendentes; `/duvidas`: lista de profissionais pro formulário de nova dúvida). **Fr13/Fr14** (troca de treino pessoal ativo sem nenhum tratamento de erro em `/meu-treino-pessoal` e `/meu-treino-pessoal/criar` — `ReplaceSelfTemplateDialog` ganhou o mesmo padrão de mensagem visível já usado em `template-preview-dialog.tsx`). **A9** (reenvio de e-mail de verificação: vazava a mensagem crua do provedor Resend num 500 quando o envio falhava, e respondia "reenviado" com sucesso mesmo quando a chave da API estava ausente e o envio foi pulado em silêncio — `sendVerificationEmail` passa a checar o retorno booleano de `sendMail`, e o controller só expõe `error.message` ao cliente quando o erro já tem `statusCode` de domínio). *Modelo: Sonnet 5. Sem migration. Sem testes novos (mudanças de UI/mensagem de erro cobertas pela suíte existente, nenhuma lógica de negócio nova), 559/559 backend, 52/52 Jest/RTL, `tsc --noEmit` limpo nos dois lados. Ver `docs/AUDITORIA-2026-07-31.md` pro apêndice de resolução desta rodada.*
- [x] Fase 109: checagem rápida e rasa (pedida explicitamente, não uma nova auditoria) atrás de inconsistências causadas por COMBINAR as correções das Fases 107/108 entre si — uma correção mudando uma premissa que outra, feita num commit diferente, ainda assumia verdadeira. 4 agentes em paralelo (billing/plan-expiry, auth, fitness/connections/admin/support, frontend), 7 achados reais confirmados manualmente e corrigidos. **O mais sério**: `/personal/upgrade` ficava sem saída pra um Personal em `past_due` — o downgrade não-terminal (B1, Fase 107) passou a manter `stripeSubscriptionId` mesmo caindo pra FREE, mas a tela decidia "assinar" vs. "gerenciar pelo Portal" só pelo tier (nunca lia `hasSubscription`, que já existia na API desde a Fase 93); a pessoa via os botões de assinar, clicava, e o guard novo do B10 rejeitava mandando "gerenciar pelo Portal" sem nenhum botão de Portal visível — corrigido gateando pela mesma condição que o backend usa (`hasSubscription`), com mensagem própria pro caso "FREE mas com assinatura pendente de atenção". **F12** (`firstMissingKey`, Fase 107) só tinha sido aplicado numa das 3 telas com o mesmo bug — as duas telas de edição de sessão continuavam escondendo o botão "Próximo" ao abrir a última sessão na ordem do esquema, mesmo com lacunas mais cedo; corrigido com fallback (posição seguinte quando existe, senão a primeira lacuna real). **`TemplatePreviewDialog`** em `/meu-treino-pessoal` não tinha recebido o padrão `isApplying`/`errorMessage` que o próprio componente ganhou na Fase 107 — fechava o diálogo no clique antes de saber se a mutation deu certo; corrigido pra só fechar em sucesso real ou ao abrir o diálogo de troca (evitando 2 overlays ao mesmo tempo). Mais 4 achados menores: `AuthGuard` de uma tela de sessão do Personal ainda liberava NUTRICIONISTA (backend já rejeitava, só UX confusa); método de repositório morto (`updateUserRole` antigo, substituído por `updateUserRoleWithAuditLog` na Fase 107) removido; comentário desatualizado no guard de rate-limit do login (citava um cenário que a própria Fase 107 já tinha tornado impossível); `client-invites.controller.ts` sem o campo `code` no erro, diferente dos controllers irmãos. *Modelo: Sonnet 5. Sem migration, sem testes novos (ajuste de gate de UI, remoção de código morto, correção de comentário — nenhuma lógica de negócio nova), 559/559 backend, 52/52 Jest/RTL, `tsc --noEmit` limpo nos dois lados. Ver `docs/AUDITORIA-2026-07-31.md` pro apêndice de resolução desta rodada.*
- [x] Fase 110: nova categoria de catálogo, **Pilates** (mesmo padrão de curadoria das Fases 15/33.3/50/51 — pesquisa real + verificação de cada `mediaUrl` via oEmbed do YouTube antes de cadastrar). Pedido específico do fundador: só pilates SOLO/em casa (mat work, sem reformer/cadillac/aparelho), vídeos curtos mostrando o movimento completo, inglês aceito. 12 exercícios clássicos do mat work (O Cem, Enrolamento de Coluna, Círculo com Uma Perna, Rolando Como uma Bola, Alongamento de Uma/Duas Pernas, Alongamento de Coluna à Frente, Nadando, Ponte de Ombro, Chute Lateral, A Serra, Tesoura Cruzada) — 2 canais estabelecidos e consistentes (Howcast e Online Pilates Classes by Lesley Logan), todos com `equipment: "Peso Corporal"`. `muscleGroup: "Pilates"` é string livre (sem enum no schema), aparece sozinho como filtro novo assim que o primeiro exercício existe — zero migration, seed novo (`prisma/seed-pilates.ts`, idempotente por nome, mesmo padrão das levas anteriores). **Achado real ao investigar o pedido do fundador** ("tentei inserir um Short e não deu"): não é restrição do YouTube — o mesmo vídeo se incorpora normalmente via `watch?v=`, só a validação deste projeto (regex em `frontend/lib/youtube.ts#getYoutubeVideoId` e `src/admin/services/admin.service.ts#YOUTUBE_URL_REGEX`) nunca reconhecia o formato `youtube.com/shorts/<id>` — corrigido nos dois lugares, agora Shorts também é aceito e incorporado normalmente. *Modelo: Sonnet 5. Sem migration. 2 testes novos (Shorts aceito no backend e no parser do frontend), 560/560 backend, 57/57 Jest/RTL, `tsc --noEmit` limpo nos dois lados. Seed rodado no banco local (12 criados, 0 duplicados) — não rodado em produção nesta sessão.*
- [x] Fase 111: bug real reportado pelo fundador — "quando o aluno edita um treino ele não consegue mudar nem o nome do programa nem o treino do dia". Investigação mostrou que não era um bug de permissão: renomear NUNCA existiu pra ninguém (nem aluno, nem Personal) — o nome de `WorkoutProgram`/`Workout` só podia ser definido na criação, sem nenhum endpoint `PATCH`/UI de edição depois (o único jeito de "trocar" era "Salvar como template", que cria um programa NOVO, não renomeia o existente). Corrigido pros dois lados, já que a causa raiz (endpoint inexistente) era a mesma: `PATCH /api/workout-programs/:id/name` e `PATCH /api/workouts/:id/name`, cada um ramificando por role no controller (mesmo padrão já usado em `deleteProgramHandler`/`addExerciseHandler` desde a Fase 85) — Personal renomeia o que é seu (`personalId`), aluno renomeia seu próprio treino `origin: SELF` (`alunoId`). Sem gate de billing pro Personal; pro aluno, renomear exige Aluno Premium vigente (mesma classificação de `moveExercise`/`deleteExercise` — editar conteúdo já existente, não expandir — só excluir o programa INTEIRO é a exceção documentada). Novo componente `frontend/components/inline-rename.tsx` (clique no lápis vira input inline, Enter salva, Esc cancela, erro visível) reaproveitado nas 4 telas (título do programa em `/programas/[id]` e `/personal/programas/[id]`; nome da sessão nas 2 telas de editor de sessão — que, como efeito colateral, passaram a MOSTRAR o nome customizado da sessão, que antes não aparecia em lugar nenhum nessas telas, só o rótulo do esquema tipo "Sessão A"). *Modelo: Sonnet 5. Sem migration. 8 testes novos de backend (ownership dos dois lados, validação de nome vazio, gate de Premium), 567/568 backend (1 falha pré-existente e não-relacionada: `GET /api/exercises` compara contagem contra `exercises_seed.json`, e o banco local ganhou +12 linhas do seed de Pilates da Fase 110 — não é regressão desta fase), 57/57 Jest/RTL, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 112: fundação de dados pro dashboard de treino (resumo pós-treino + histórico) que o fundador quer construir no estilo SmartGym — pedido explícito foi planejar ANTES de desenhar tela nenhuma, já que o app não capturava nenhuma métrica de esforço/duração real (só séries/reps/carga). Plano revisado junto com o fundador, escopo travado em capturas sem wearable (Health Connect/HealthKit ficaram fora desta rodada, análise completa registrada no histórico da sessão pra retomar se um dia fizer sentido). **Fundação de schema**: nova tabela `WorkoutSessionLog` (1 linha por CONCLUSÃO real de sessão — antes não existia nenhum registro durável, o resumo pós-treino inferia "esta sessão" por uma janela heurística de 6h contra `SetLog.loggedAt`) — `onDelete: Cascade` em `Workout` (mesmo padrão de `WorkoutTranslation`, um log sem o treino que descreve não tem significado próprio). **Captura barata, sem hardware**: duração real (já calculada 100% no client desde a Fase 89, nunca chegava no backend — `POST /complete` agora aceita `durationSeconds` opcional) + RPE (Percepção Subjetiva de Esforço, escala Borg 0-10, simplificada pra 5 níveis com emoji na UI) numa pergunta ÚNICA e OPCIONAL depois do resumo pós-treino (nunca bloqueia a conclusão em si — decisivo pro caminho de auto-encerramento por inatividade, onde não há ninguém presente pra responder), gravado via `PATCH /api/workout-sessions/:id/rpe` separado. Combinados, dão o método de "carga de treino" de Foster (RPE × duração) sem NENHUM sensor. **Dashboard histórico novo** em `/evolucao` (aluno) via `GET /api/progress/session-history`: 2 gráficos de tendência (duração real, carga de treino — nunca um só com 2 eixos, escalas incompatíveis) + 1 barra segmentada de distribuição de esforço entre sessões recentes. **Extensão pro Personal praticamente de graça**: `/personal/alunos/[alunoId]` já reaproveitava os mesmos gráficos de evolução do aluno (achado da Fase 29) — o novo endpoint já aceita `alunoId` pelo mesmo `assertAluno` que os outros 3 endpoints de `/api/progress` já usavam, então a extensão foi só chamar o mesmo componente 2x nessa tela, zero autorização nova escrita. **Crítica de design registrada**: a skill de referência do fundador (`thunderafit-dashboard-design`) ilustra FC contínua intra-sessão e zona-de-FC intra-sessão — sem wearable, esses 2 padrões não têm dado de sustento (RPE é 1 número por sessão inteira, não uma amostra contínua); a barra de "zona" foi reaproveitada com semântica ajustada (distribuição ENTRE sessões, não dentro de 1), e o padrão de curva contínua ficou de fora do V1 de propósito, em vez de forçar com dado inventado ou vazio. *Modelo: Sonnet 5. 1 migration aditiva (`WorkoutSessionLog`). 12 testes novos de backend (persistência de duração, RPE com validação/ownership, `session-history` incluindo a visão do Personal), 4 novos de frontend (RPE picker no modal, distribuição de esforço), 576/577 backend (mesma falha pré-existente da Fase 111/110, não-relacionada), 61/61 Jest/RTL, `tsc --noEmit` limpo nos dois lados.*
- [x] Fase 113: pedido do fundador pra reforçar uma regra do projeto (a partir de agora, toda tela/botão novo precisa existir em PT/EN/ES) + achado real que ele suspeitava: a curadoria de Pilates (Fase 110) tinha criado os 12 exercícios só em português, sem nenhuma linha de `ExerciseTranslation` — usuários em EN/ES caíam no fallback pro texto PT cru (nunca quebra, `exercise-translation.service.ts` já tem esse fallback por design, mas sub-traduzia o catálogo silenciosamente). **Checagem da regra**: comparação estrutural das 1081 chaves de `pt.json`/`en.json`/`es.json` confirma paridade total (nenhuma chave faltando em nenhum arquivo) — as telas/componentes novos da Fase 112 já nasceram 100% traduzidos, nenhum ajuste necessário aí. **Achado real corrigido**: `prisma/seed-traducoes-pilates.ts` (mesmo padrão de `seed-traducoes-treino-em-casa.ts`) — 24 linhas novas de tradução (12 exercícios × EN/ES, nome+descrição traduzidos por um humano/IA, não tradução literal palavra-por-palavra; `muscleGroup: "Pilates"` idêntico nos 3 idiomas, termo internacional). **Achado adicional registrado, não corrigido nesta fase** (fora do pedido específico): os outros 2 lotes recentes de curadoria (`seed-cardio-alongamento.ts`, `seed-antebraco-trapezio-quadril.ts`) têm a MESMA lacuna — nenhum dos dois populou `ExerciseTranslation` na época. *Modelo: Sonnet 5. Sem migration. Seed de tradução rodado no banco local (24 criadas, 0 puladas) — produção pendente, mesmo padrão manual das curadorias anteriores. 8/8 testes de `exercise-translation.test.ts` continuam verdes, `tsc --noEmit` limpo.*
- [x] Fase 114: pedido do fundador pra fechar os 2 achados deixados em aberto na Fase 113 — os outros 2 lotes de curadoria de exercícios sem tradução. `prisma/seed-traducoes-cardio-alongamento.ts` (22 exercícios × EN/ES = 46 linhas: 10 de Cardio, `muscleGroup` idêntico nos 3 idiomas como Pilates; 12 de Alongamento, `EN: "Stretching"`/`ES: "Estiramiento"`) e `prisma/seed-traducoes-antebraco-trapezio-quadril.ts` (16 exercícios × EN/ES = 32 linhas cobrindo Antebraço/Trapézio/Flexores do Quadril — 3 exercícios desse lote, "Wrist Roller", "Elevação de Joelho em Pé (Marcha Alta)" e "Elevação de Joelho Sentado no Banco", já tinham sido traduzidos na Fase 53 por reaparecerem num programa curado à parte, então não foram duplicados aqui — o check de idempotência já pularia mesmo se estivessem). Mesmo padrão de nome+descrição natural (não tradução literal) usado nas Fases 53/113. *Modelo: Sonnet 5. Sem migration. Seeds rodados no banco local (46 + 32 = 78 traduções criadas, 0 puladas) — produção pendente, mesmo padrão manual de todo seed de dado desta sessão. 8/8 testes de `exercise-translation.test.ts` continuam verdes, `tsc --noEmit` limpo.*
- [x] Fase 115: pedido do fundador pra levantar o catálogo real de `WorkoutProgram` (origin: SELF, isTemplate: true) e, a partir disso, criar 12 programas novos "de acordo com as tags, identificando quais têm menos". Levantamento direto no banco (excluindo fixtures de teste E2E que poluíam a contagem — "Fase 63 — Treino com Tags"/"Template Bloco E2E"/"Template Pessoal E2E"/"Treino Tags E2E", só nomes e tags de teste, 0 sessões, sem relação com conteúdo real): das 7 `WorkoutTag` do enum (Fase 63), AVANCADO(10)/INTERMEDIARIO(8)/EXPRESS(5)/FEMININO(4)/INICIANTE(3) já tinham cobertura real, mas **HIPERTROFIA e DEFINICAO nunca haviam sido aplicadas a nenhum programa real — 0 cada**, apesar de existirem no enum desde a criação do sistema de tags. `prisma/seed-programas-hipertrofia-definicao.ts`: 12 programas PREMIUM novos (mesmo padrão de `seed-programas-feminino-avancado.ts`), HIPERTROFIA e DEFINICAO aparecendo em 7 programas cada (2 tags sozinhas, 2 combinadas como "recomposição corporal"), reforçando de passagem também INICIANTE (+3) e FEMININO (+3). Nomes escolhidos em inglês/termos internacionais de fitness (Prime Mass, Hybrid Volume, Titan Hypertrophy, Lean Start, Shred Circuit, Cut Elite, Glow Tone, Curve Builder, Body Recomp, Vanguard Recomp, Quick Mass, Sculpt Elite) — mesmo padrão já usado no catálogo ("Monster Mass", "V-Taper Master") que funciona nos 3 idiomas do app sem precisar de tradução própria de nome. Cada programa tem 2 a 5 sessões (ABC.../letras) com 5-6 exercícios cada, todos resolvidos contra o catálogo real (nenhum "não encontrado" no seed), e observações técnicas (drop-set, rest-pause, cluster set, bi-set, pico de contração, finalizadores metabólicos/HIIT) na maioria dos exercícios de cada sessão. Descrição/nomes de sessão/observações ficam em PT (canônico) — tradução de `WorkoutProgramTranslation` fica de fora desta rodada, mesmo padrão manual separado das Fases 53/59 (o pedido desta vez era especificamente nomes universais, não tradução completa do conteúdo). *Modelo: Sonnet 5. Sem migration. Seed rodado no banco local (12 programas, 34 sessões, 0 exercícios pulados) — produção pendente, mesmo padrão manual de todo seed de dado desta sessão. 145/145 testes relevantes (`admin-self-templates`, `admin-user-premium`, `self-workout-builder`, `aluno-premium`, `workout-programs`) continuam verdes, `tsc --noEmit` limpo.*
- [x] Fase 116: pedido do fundador pra repetir o levantamento da Fase 115 e criar mais 18 programas, "sendo 3 deles free". Novo levantamento pós-Fase 115 (mesma exclusão de fixture de teste E2E): AVANCADO(14)/INTERMEDIARIO(13) já bem cobertas; FEMININO/EXPRESS/HIPERTROFIA/DEFINICAO empatadas em 7; **INICIANTE era a mais escassa isolada, com só 6**. `prisma/seed-programas-lote2.ts`: 18 programas novos, 15 `category: PREMIUM` + 3 `category: PRONTOS` (gratuitos, sem cadeado de Aluno Premium — mesma categoria dos 3 templates gratuitos já existentes, "Shape V: Hipertrofia (ABCD)" etc.). Nível concentrado em INICIANTE (11 dos 18, incluindo os 3 gratuitos) pra corrigir a lacuna identificada, com só 5 INTERMEDIARIO e 2 AVANCADO novos (as duas que já tinham mais volume); tags de objetivo (HIPERTROFIA/DEFINICAO/FEMININO/EXPRESS) distribuídas entre os 18 programas. Mesmo padrão de nomes internacionais da Fase 115 (Foundation Full Body, Metabolic Starter, Balanced Push Pull, Steel Foundations, Gentle Definition, Curve Start, Rapid Start, Fresh Recomp, Femme Express, Iron Basics, Clean Cut, Toned Beginnings, Momentum Mass, Definition Engine, Curve & Cut, Balanced Athlete, Peak Definition, Apex Mass) e mesma estrutura de sessões/observações técnicas. **Resultado final confirmado por query direta**: AVANCADO 16, INTERMEDIARIO 18, INICIANTE 17, HIPERTROFIA 16, DEFINICAO 15, FEMININO 11, EXPRESS 11 — distribuição bem mais equilibrada entre as 7 tags (antes: 3 a 14). *Modelo: Sonnet 5. Sem migration. Seed rodado no banco local (18 programas, 44 sessões, 0 exercícios pulados) — produção pendente, mesmo padrão manual de todo seed de dado desta sessão. 145/145 testes relevantes continuam verdes, `tsc --noEmit` limpo.*
- [x] Fase 117: o fundador relatou como bug "ativei 7 dias do premium e quando terminou não tem a opção de gerenciar plano nem assinar, só diz assinatura em breve". **Investigação separou duas coisas diferentes.** (1) **Não é bug**: o checkout pago do Aluno Premium nunca foi construído — está documentado em 4 lugares (`src/billing/routes/billing.routes.ts:22`, `src/billing/AGENTS.md:32/55/62/66`), incluindo a instrução explícita *"Don't build a checkout session against these until that phase starts"* e as palavras do próprio fundador *"vamos refinar isso quando colocarmos o pagamento em produção"*. Só existem 2 rotas de aluno (`GET /premium-status`, `POST /trial`); `checkout-session`/`portal` retornam 403 pra ALUNO, e alunos nunca ganham `stripeCustomerId`/`stripeAlunoSubscriptionId` — por isso não há "gerenciar plano" (não existe nada pra gerenciar). O `computeEntitlement` e a mensagem exibida estão corretos; o que existe é uma **lacuna de produto** (quem gastou o teste não tem como pagar), não um defeito. Desbloqueio atual: concessão manual pelo admin (`PUT /api/admin/users/:id/premium`), que funciona independente do teste já usado e, sem `days`, usa a sentinela de 100 anos. (2) **Bug REAL encontrado durante a investigação, esse sim corrigido**: `frontend/app/nimbus/usuarios/page.tsx` derivava "tem Premium?" do ALUNO só de `alunoPremiumStatus !== "NONE"`, **ignorando `alunoPremiumExpiresAt`**. Como NADA reescreve o status pra `NONE` quando vence (por design — não existe cron, a verdade é computada na leitura, ver `aluno-premium.service.ts:20-24`), um aluno com teste vencido aparecia no painel admin como **"Premium ativo (permanente)" pra sempre** — e como `daysUntil` devolve `null` pra data passada, até o prazo desaparecia do rótulo, reforçando a leitura de "permanente". O painel contradizia o app, que negava o acesso corretamente; possivelmente foi isso que confundiu o diagnóstico inicial. Lado PERSONAL/NUTRICIONISTA **não** tinha o problema de forma permanente porque `revertExpiredPersonalPlan` (`src/lib/plan-expiry.ts`) reescreve `planoAssinatura` no banco quando vence (defasagem transitória, se cura sozinha). Correção extraiu o predicado pra `frontend/lib/premium.ts#hasActivePremium`, documentando a **assimetria proposital** entre os dois roles: pro ALUNO prazo ausente = SEM acesso (concessão do admin sempre grava sentinela de 100 anos, teste sempre grava 7 dias); pro PERSONAL prazo nulo = PERMANENTE (assinatura real via Stripe). (3) **Divergência de doc corrigida de passagem**: `src/billing/AGENTS.md` dizia `990` centavos e `30%` de desconto trimestral, mas a Fase 87 já havia mudado pra `999` e `20%` (`src/billing/stripe.ts:99-103`) — risco real de alguém construir o checkout futuro em cima do número errado. *Modelo: Opus 5. Sem migration. Correção 100% no frontend (os 2 campos já vinham da API, `admin.repository.ts:72-73`) e sem nenhuma chave nova de i18n — o estado vencido cai no `statusNone` que já existia nos 3 idiomas, então a regra PT/EN/ES continua satisfeita sem tradução nova. 14 testes novos (`frontend/lib/__tests__/premium.test.ts`, cobrindo teste vencido com status preso, prazo ausente, data inválida e a assimetria dos dois roles), 75/75 Jest/RTL no frontend (era 61), `tsc --noEmit` limpo, ESLint limpo nos arquivos tocados (os 8 erros restantes do `npm run lint` são pré-existentes em `e2e/` e `proxy.ts`, não introduzidos aqui).*
- [x] Fase 118: primeiro lote de correções da auditoria 2026-08-06 (`docs/AUDITORIA-2026-08-06.md`) — critério de agrupamento: **tudo que tinha correção clara, sem decisão de produto pendente e sem migration**. Os 8 achados que não cabiam nesse critério foram planejados um a um em `docs/PROXIMAS-FASES-AUDITORIA.md` (A4, M4, M6-Terraform, B6, B7, B9, B10, B11), com o trade-off de cada opção escrito — não é backlog vago. **Os 3 de risco ALTO**: (A1) o overlay do resumo pós-treino era `fixed` sem `overflow-y-auto` e o `RpeQuickPicker` da Fase 112 empurrou os botões pra fora da viewport em telas ≤375px (iPhone SE, 360×640, 320×568) — **não havia como fechar, compartilhar nem baixar**, só recarregar perdendo o resumo; agora rola (`items-start` + `my-auto` mantêm centrado quando cabe). (A2) template SELF **sem nenhuma sessão** era card aplicável no catálogo do aluno — `createSelfTemplate` cria o programa e as sessões vêm depois, então durante toda a curadoria do admin ele já estava exposto, e aplicar substituía o treino real do aluno por um programa vazio, apagando histórico de `SetLog` sem desfazer; defesa em 2 camadas (filtro `workouts: { some: {} }` na listagem + 409 no `applySelfTemplate`, este DEPOIS do gate de PREMIUM pra não vazar estado do template a quem não tem direito e não atrasar o erro acionável). (A3) o `RpeQuickPicker` gravava 2/4/6/8/10 contra as faixas Borg do backend (`<=3` leve, `<=6` moderado, senão intenso), então **"Leve" (4) aparecia como "Moderado"** na barra de /evolucao e a faixa "Leve" era impossível de reportar — corrigido no valor emitido (2/3/5/7/9), não nos limiares, porque o padrão Borg está certo e quem divergia era o picker; o Personal lia essa mesma barra pra decidir carga. **MÉDIOS**: (M1) `new Date("YYYY-MM-DD")` é meia-noite **UTC**, então o lembrete de pagamento exibia um dia ANTES do escolhido em todo fuso a oeste de UTC (todo o Brasil) e contradizia o campo de data no mesmo card — extraído pra `frontend/lib/date-input.ts` com conversão pelo fuso local nos dois sentidos (o `slice(0,10)` que existia erra na direção oposta a leste de UTC). (M2) `connections.service.ts` era o **único** ponto de decisão de autorização do backend que lia `planoAssinatura` cru, sem `revertExpiredPersonalPlan` — um Personal com cortesia BASE/PLUS vencida conseguia RELIGAR a presença no diretório público e ainda ser rankeado acima dos FREE (o B7/C2 da auditoria anterior fechou só a direção "desligar"); é a mesma classe do bug da Fase 117, no backend. (M3) em `/programas/[id]` a falha do status de Premium fazia `canEdit` virar false e a tela **apagava os controles de edição em silêncio**, sem erro nem retry — um aluno COM Premium concluía que perdeu o acesso (classe do Fr13, em local novo criado pela própria correção do F7). (M5) `durationSeconds` negativo respondia 400 e **travava a conclusão do treino**: com o relógio do aparelho atrasado o aluno não conseguia concluir, e o retry falhava igual porque o `startedAt` persiste no `localStorage` — contradizia a intenção declarada ("nunca 400 sem ele"); agora sanitiza (descarta inválido, teto de 24h que também protege a coluna `Int4` do overflow verificado contra o Postgres real). **BAIXOS**: B1 (`["session-history"]` não era invalidada por ninguém — nem ao concluir, nem ao responder RPE), B2 (gráfico de carga de treino renderizava área **em branco** sem RPE, nas 2 telas), B3 (os 3 `PATCH` novos davam 500 com corpo vazio em vez de 400 — destructuring sem `?.`, e 2 deles **fora** do `try`), B4 (nomes sem teto de tamanho nem checagem de tipo em 7 pontos, centralizados em `src/lib/validate-name.ts`; a Fase 111 tornou o nome mutável a qualquer momento, o que mudou o perfil de risco), B5 ("Baixar imagem" falhava **em silêncio** — `try/finally` sem `catch` — e o fallback do compartilhar gerava unhandled rejection prometendo um download que não aconteceu), B8 ("Cancelar" no RoleEditor/PremiumEditor não descartava o estado pendente: escolher ADMIN, cancelar e reabrir deixava a promoção a **um clique**, anulando o passo de confirmação). **i18n**: I1 (enum `Role` cru no `<option>` do Nimbus — rótulos singulares próprios, não as chaves plurais de filtro, que leriam errado) e I2 (`kg` concatenado no JSX). **Drift de documentação, todo corrigido**: `fitness/AGENTS.md` negava a existência do `WorkoutSessionLog` justamente na seção *Handle with care* (um agente lendo aquilo deixaria de gravar o log e o dashboard ficaria vazio); `billing/AGENTS.md` **proibia** o corte retroativo que a Fase 103 já entregou e dizia "monthly and annual" quando o código só tem monthly/quarterly (construir "anual" cobraria 1 mês vendendo 1 ano); `.env.example` documentava vars `_ANNUAL` que o código nunca lê; `MASTER_SPEC.md` tinha o preço pré-Fase-87 (990/30% em vez de 999/20% — gêmeo do bug corrigido na Fase 117, não propagado), `PAGO`/limite 50 extintos, paginação 300/500 (é 1000/2000), e listava verificação de e-mail / reset de senha / login Google como "fora do escopo"/"adiado" embora entregues nas Fases 77/81 — justamente as seções usadas pra decidir o que NÃO construir; `notifications/AGENTS.md` dizia poll de 30s (é 6h desde a Fase 102 — erro de 720×); `admin/AGENTS.md` dizia 1 ação auditada (são 4); `connections/AGENTS.md` auditava o payload público contra allowlist errada (citava `location`, campo morto, e omitia 4 campos realmente expostos); `infra/README.md` afirmava que `gcloud` não exige WSL (contradizendo o `AGENTS.md`, que existe pra evitar exatamente esse engano) e mandava preencher **3** secrets quando o Terraform cria **6** — seguir o setup à risca subia produção com e-mail transacional e billing quebrados em silêncio. **Não corrigido de propósito**: o guard de idempotência do `/complete` (A4) — só o comentário enganoso foi corrigido, marcado com ⚠️, porque a escolha da semântica precisa de decisão e um fix errado quebra conclusão legítima. *Modelo: Opus 5. Sem migration. 3 helpers novos (`src/lib/validate-name.ts`, `frontend/lib/date-input.ts`, e o teste do picker), 2 chaves i18n novas nos 3 idiomas + rótulos de role (paridade re-verificada por parse real: **1089/1089/1089** chaves-folha, zero divergência). 17 testes novos (7 do mapeamento RPE que trava o acoplamento picker↔backend, 6 do fuso, 3 do A2, e o teto de duração); 3 fixtures de teste corrigidos que **asseveravam o comportamento buggy** (template sem sessão sendo listado/aplicado, e o RPE 6 do "Moderado") — mais 2 blocos de cleanup que violavam FK ao apagar programa antes das sessões. 88/88 Jest/RTL no frontend (era 75), 582/583 backend — a única falha é a mesma pré-existente das Fases 110/111/112 (`GET /api/exercises` compara a contagem contra `data/exercises_seed.json`, 255 entradas, enquanto o banco local tem 267 por causa das curadorias que não atualizaram o JSON); nenhum arquivo de exercício/seed está neste diff. `tsc --noEmit` limpo nos dois lados, ESLint limpo nos arquivos tocados (o 1 erro restante é pré-existente em `treinos/[id]/page.tsx`, confirmado no HEAD).*
- [x] Fase 119: 2ª rodada da auditoria 2026-08-06 + correção da inconsistência de banco, com **acesso ao banco de PRODUÇÃO autorizado explicitamente pelo fundador** ("hoje produção não tem alunos reais então pode executar"). **Auditei prod antes de mexer em nada, e isso corrigiu suposições minhas registradas em fases anteriores**: os seeds que eu havia marcado como "produção pendente" nas Fases 110/113/114/115/116 JÁ haviam sido rodados — Pilates presente, Fase 115 12/12, Fase 116 18/18, traduções EN/ES=247 — e prod tem **zero** fixture de teste (confirma que a poluição do B10 é local-only). Prod: PG 18.4, 11 MB, 8 ALUNO / 3 PERSONAL / 1 NUTRICIONISTA / 2 ADMIN, 32 SetLog. **Inconsistência de banco, causa raiz**: os 12 exercícios de Pilates existiam no banco mas NÃO em `data/exercises_seed.json`, porque `seed-pilates.ts` (Fase 110) inseria direto no banco sem entrar no JSON. Duas consequências reais, não só o teste vermelho: (a) o teste de contagem do catálogo falhava desde a Fase 110, **mascarando qualquer regressão real** naquele endpoint; (b) um ambiente novo (dev/CI) nascia com 255 exercícios contra 267 de prod, e como os programas curados das Fases 115/116 resolvem exercício **por nome** e pulam com aviso quando não acham, perdiam silenciosamente as linhas de Pilates. Corrigido tornando o JSON a fonte única: os 12 foram extraídos de prod e anexados (96 inserções, **0 remoções** — nenhuma entrada existente tocada), e `seed-pilates.ts` ficou marcado como histórico. **Achado NOVO, só visível auditando prod**: 20 exercícios sem NENHUMA tradução EN/ES (sobras avulsas das curadorias, não um lote inteiro faltando — e zero com tradução parcial). `prisma/seed-traducoes-lacunas.ts` cobre 21 (o local tinha um a mais que prod), **aplicado em produção: 20 → 0 sem tradução, +40 linhas, exercícios inalterados em 267**. **A4 (era o único ALTO em aberto)**: `/complete` agora é idempotente dentro de `COMPLETE_DEDUPE_MS = 2 min`. Duas nuances que só apareceram implementando: o guard também **não move `lastCompletedAt` de novo** (mover a fronteira é o que destrói a janela do resumo — só bloquear o `create` deixaria o defeito de pé pela metade), e numa reentrega o resumo é **reconstruído com a janela da chamada original** usando as 2 conclusões mais recentes do próprio `WorkoutSessionLog`, porque sem isso o retry (que é o caminho DELIBERADO do fix do Fr1/Fr4 em falha de rede) devolvia volume 0 depois de um treino real — o mesmo defeito, deslocado da tabela pra resposta. **M4**: `ClientInvite` entrou no cascade de `user-deletion.ts` pelos DOIS lados (`personalId` e `consumedByAlunoId`), fechando **PII que sobrevivia à exclusão de conta** (`label` é texto livre, tipicamente o nome de quem foi convidado) — a regra que exige isso estava escrita em `src/admin/AGENTS.md` e a Fase 104 a quebrou. Verificado em prod: **0 convites**, então é preventivo, sem resíduo a limpar. **Correção de qualidade no meu próprio código**: ao exportar `aplicarTraducoes` pra reaproveitar em prod sem duplicar as 21 traduções, o `main()` do seed passou a rodar como efeito colateral do import (inofensivo por ser idempotente, mas surpresa num script que escreve no banco) — resolvido com `require.main === module`. **Dois defeitos ADICIONAIS que só apareceram porque a cobertura de tradução chegou a 100%** (a suíte completa ficou vermelha depois das correções acima, e investigar levou a eles): (1) `exercise-translations.repository.ts#upsert` **escrevia sem invalidar o próprio cache** de 5 min — o `invalidateCache()` existia no arquivo sem nenhum chamador, então qualquer processo que escrevesse E lesse traduções servia dado obsoleto; ficava latente porque dependia da ORDEM das suítes (uma anterior precisava ter aquecido o cache daquele locale), e as minhas mudanças reordenaram os arquivos e expuseram. (2) `exercise-translation.test.ts` **sequestrava o 1º exercício do catálogo real** e (a) apagava as traduções DE VERDADE dele no `afterAll` — a cobertura do banco local regredia de 0 pra 1 a cada `npm test`, desfazendo a correção desta própria fase — e (b) **assumia que aquele exercício não tinha tradução**, premissa que só valia enquanto o catálogo estava incompleto: ao fechar a lacuna, 4 testes passaram a falhar (o de fallback recebia a tradução real em vez do PT, e o `create()` batia na unique constraint). Refatorado pra criar um exercício PRÓPRIO e descartável, então a suíte controla 100% do estado e não toca em nada real. *Modelo: Opus 5. Sem migration. 6 testes novos (4 do A4 cobrindo dedupe, volume real na reentrega, `lastCompletedAt` intacto e conclusão legítima FORA da janela; 2 do M4 cobrindo os dois lados do cascade) — o A4 não tinha teste nenhum de dupla chamada, e foi essa ausência que deixou o defeito passar. Todo acesso a produção foi somente-leitura primeiro (medir antes de mutar), as escritas foram exclusivamente ADITIVAS e idempotentes, e a limpeza de órfãos rodou em dry-run. `data/exercises_seed.json` 255 → 267, batendo com prod. **589/589 backend em 37/37 suítes — verde completo pela primeira vez nesta sessão**: a falha de contagem do catálogo que se arrastava desde a Fase 110 foi ELIMINADA (o JSON virou fonte única), não silenciada, e as 4 falhas novas que apareceram no caminho eram os 2 defeitos de cache/isolamento descritos acima. 88/88 Jest/RTL no frontend, `tsc --noEmit` limpo nos dois lados. Scripts temporários de auditoria removidos.*
